var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Documentation index","text":""},{"location":"index.html#use-it","title":"Use it","text":"Document Purpose getting-started.md Prerequisites, install, configure, run, verify (CLI, server, Docker) cli.md Local CLI guide: commands, approvals, exit codes, credential stores api-reference.md Endpoints, statuses, SSE events, webhook auth configuration.md Every environment variable connectors.md What each connector can do and how to set it up"},{"location":"index.html#understand-it","title":"Understand it","text":"Document Purpose architecture.md Components, data flow, agent loop, module map guardrail-policy.md How a verdict is produced, policy packs, sensitive input contracts.md Runtime schemas (v0.1) data-model.md Database tables and local SQLite/JSONL stores audit-design.md Current audit model vs. the event-sourced target glossary.md Terms used across the docs"},{"location":"index.html#run-and-secure-it","title":"Run and secure it","text":"Document Purpose deployment.md Deploy, migrate, monitor, back up, runbook security.md Threat model, trust boundaries, hardening checklist limitations.md Known gaps and inconsistencies"},{"location":"index.html#build-on-it","title":"Build on it","text":"Document Purpose contributing.md Workflow, where code goes, adding a connector testing.md Test layout, fixtures, how to run and write tests decisions/ ADRs: 0001 foundation, 0002 local-first CLI, 0003 embedded guardrail engine, 0004 audit granularity (proposed; would amend 0002's write-once audit)"},{"location":"index.html#integration-guides-existing","title":"Integration guides (existing)","text":"<p>integrations/guardrail.md, integrations/stripe.md, integrations/telegram.md, CHANGELOG-atomic-browser-audit.md.</p>"},{"location":"CHANGELOG-atomic-browser-audit.html","title":"Merge: atomic browser audit logging (from <code>laples</code>) into <code>main</code>","text":"<p>This merge ports the per-action audit-logging feature developed on the <code>laples</code> branch into <code>main</code>, without touching anything <code>main</code>-only (the <code>fe/</code> demo UI and the <code>ASK_USER</code> clarification flow are untouched).</p>"},{"location":"CHANGELOG-atomic-browser-audit.html#what-changed","title":"What changed","text":"<ol> <li><code>app/config/settings.py</code></li> <li> <p>New flag <code>ATOMIC_BROWSER_AUDIT: bool = False</code>. Off by default so      existing behavior/tests are unaffected until opted in per environment.</p> </li> <li> <p><code>app/domains/agent/services/browser_prototype_agent.py</code></p> </li> <li>New function <code>run_browser_prototype_agent_atomic()</code>: runs a batch of      browser steps in one Playwright session (same session-reuse as the      existing combined path) but writes one <code>audit_logs</code> row per step      (own <code>action_id</code>, shared <code>run_id</code>) instead of one combined row for the      whole batch. Purely additive \u2014 nothing calls it unless the flag is on.</li> <li> <p>Small bugfix carried over from <code>laples</code>: <code>_write_audit()</code> now uses      <code>get_audit_repository()</code> (respects <code>settings.AUDIT_BACKEND</code>) instead of      hardcoding <code>AuditRepositoryDB()</code>.</p> </li> <li> <p><code>app/domains/agent/services/agent_loop.py</code></p> </li> <li>New <code>_execute_browser_batch_atomic()</code> helper.</li> <li><code>_execute_browser_batch()</code> now gates on the flag, right after the      existing \"no URL\" check and the <code>executing</code> event emit, and after <code>main</code>'s own sanitize/<code>ASK_USER</code> handling further up the call chain \u2014      so clarification pauses still happen before any atomic execution:      <pre><code>if get_settings().ATOMIC_BROWSER_AUDIT:\n    return await _execute_browser_batch_atomic(run, steps, url, actions)\n</code></pre></li> <li> <p>On a navigation-level failure (before any per-step row can be written),      every step in the batch is written as a <code>FAILED</code> row individually so      nothing silently disappears from the audit trail. On a mid-batch action      failure, the remaining steps are written <code>SKIPPED</code>.</p> </li> <li> <p>Tests (<code>tests/unit/test_agent_loop.py</code>)</p> </li> <li><code>laples</code> had no dedicated atomizer tests (its differing tests were only      due to missing <code>ASK_USER</code>/sensitive-field features, which don't apply      here), so two new tests were added:<ul> <li><code>test_atomic_browser_audit_writes_one_row_per_step</code></li> <li><code>test_atomic_browser_audit_marks_remaining_steps_skipped_on_failure</code></li> </ul> </li> <li>The one existing test that stubs <code>get_settings()</code> wholesale was updated      to include <code>ATOMIC_BROWSER_AUDIT=False</code> so it keeps exercising the      non-atomic path unchanged.</li> </ol>"},{"location":"CHANGELOG-atomic-browser-audit.html#what-was-intentionally-not-ported","title":"What was intentionally NOT ported","text":"<ul> <li><code>laples</code>'s <code>.env*</code>, <code>Dockerfile</code>, <code>docker-compose.yml</code> \u2014 local/environment   config, not a feature.</li> <li><code>database-url-fallback.patch</code> \u2014 small unrelated docker-compose fix from   <code>laples</code>; can be applied separately if wanted.</li> <li><code>laples</code>'s <code>artifacts/audit/events.jsonl</code> and <code>data/browser/screenshots/</code>   \u2014 local run artifacts, not source.</li> </ul>"},{"location":"CHANGELOG-atomic-browser-audit.html#verification","title":"Verification","text":"<ul> <li><code>python -m py_compile</code> clean on all three modified modules.</li> <li><code>pytest tests/unit</code> \u2014 same 7 pre-existing (environment/dependency-related,   unrelated to this change) failures as unmodified <code>main</code>; all other tests   pass, including the 2 new atomizer tests.</li> </ul>"},{"location":"CHANGELOG-atomic-browser-audit.html#rollout","title":"Rollout","text":"<ol> <li>Merge with <code>ATOMIC_BROWSER_AUDIT=False</code> (default) \u2014 no behavior change.</li> <li>Enable in staging (<code>.env.staging</code>), run a few multi-step browser flows,    confirm <code>audit_logs</code> rows are now one-per-action for those runs.</li> <li>Enable in production once verified.</li> </ol>"},{"location":"api-reference.html","title":"API reference","text":"<p>Base path: <code>/api/v1</code>. FastAPI generates interactive docs at <code>/docs</code> and the OpenAPI spec at <code>/openapi.json</code>; treat those as the source of truth for exact request and response bodies. CORS allows all origins (no credentials). There is no user authentication (see security).</p>"},{"location":"api-reference.html#endpoints","title":"Endpoints","text":"Tag Method Path Purpose system GET <code>/</code> (no prefix) Service name, env, version health GET <code>/health</code> <code>{\"status\":\"ok\"}</code> chat POST <code>/chat/parse</code> Turn a prompt into a plan of atomic steps (does not execute) chat POST <code>/chat/execute</code> Start a reactive run in the background; returns <code>run_id</code> chat POST <code>/chat/execute/stream</code> Same run, streamed as Server-Sent Events chat GET <code>/chat/execute/{run_id}</code> Live run state and per-step status chat POST <code>/chat/execute/{run_id}/respond</code> Answer a paused step runs GET <code>/runs</code> Runs with action counts and latest status runs GET <code>/runs/{run_id}/actions</code> Audit events for one run actions POST <code>/actions/run</code> Run a single guarded action proposal actions POST <code>/actions/prototype/browser</code> Browser prototype path actions GET <code>/actions/{action_id}</code> One action chain (404 if unknown) audits GET <code>/audits?run_id=</code> Audit events, optionally for one run audits GET <code>/audits/latest</code> Latest audit event, or <code>{}</code> approvals GET <code>/approvals</code> Audit records in <code>PENDING_APPROVAL</code> benchmark GET <code>/benchmark</code> <code>action_count</code>, <code>avg_total_ms</code>, <code>latest_status</code> oauth GET <code>/oauth/status</code> Connection status per provider oauth GET <code>/oauth/{provider}/authorize</code> Start OAuth: <code>github</code>, <code>gmail</code>, <code>calendar</code> oauth GET <code>/oauth/{provider}/callback</code> OAuth redirect target stripe POST <code>/stripe/webhook</code> Signed Stripe events telegram POST <code>/telegram/webhook</code> Inbound Telegram updates"},{"location":"api-reference.html#chat-runs","title":"Chat runs","text":""},{"location":"api-reference.html#start","title":"Start","text":"<p><code>POST /chat/execute</code> with <code>{\"prompt\": \"...\"}</code> returns <code>{\"run_id\", \"status\", \"prompt\"}</code> immediately; the run continues in the background.</p>"},{"location":"api-reference.html#inspect","title":"Inspect","text":"<p><code>GET /chat/execute/{run_id}</code> returns the overall <code>RunStatus</code> and each step's <code>StepStatus</code>, so you can see which step is waiting. <code>GET /runs/{run_id}/actions</code> returns the audit trail.</p>"},{"location":"api-reference.html#respond-to-a-paused-step","title":"Respond to a paused step","text":"<pre><code># approve or decline\ncurl -X POST localhost:8000/api/v1/chat/execute/$RUN/respond -H \"Content-Type: application/json\" \\\n     -d '{\"step_index\":0,\"action\":\"approve\"}'\n\n# supply input for a sanitize step\ncurl -X POST localhost:8000/api/v1/chat/execute/$RUN/respond -H \"Content-Type: application/json\" \\\n     -d '{\"step_index\":0,\"action\":\"input\",\"fields\":{\"password\":\"...\"}}'\n</code></pre> <p><code>action</code> is <code>approve</code>, <code>decline</code>, or <code>input</code>. <code>input</code> requires <code>fields</code> or <code>text</code>; the other two must not send them. The response is <code>{\"run_id\",\"step_index\",\"action\",\"status\":\"accepted\",\"step_status\"}</code>.</p>"},{"location":"api-reference.html#stream-events","title":"Stream events","text":"<p><code>POST /chat/execute/stream</code> keeps the connection open and emits SSE events:</p> Event Meaning <code>planning</code>, <code>replanning</code> Planner is producing (or revising) steps <code>guardrail</code> A verdict for a step <code>step_status</code> A step changed status (<code>index</code>, <code>status</code>) <code>executing</code> A step or browser batch started <code>awaiting_approval</code> Step paused for approve/decline <code>awaiting_input</code> Step paused for user input <code>done</code>, <code>error</code> Run finished or failed <p>A heartbeat is sent every <code>SSE_HEARTBEAT_SEC</code>. Answer paused steps with <code>/respond</code>; the stream resumes.</p>"},{"location":"api-reference.html#audit-queries","title":"Audit queries","text":"<pre><code>curl \"localhost:8000/api/v1/audits?run_id=run_634a174c8449\"\ncurl localhost:8000/api/v1/audits/latest\ncurl localhost:8000/api/v1/actions/act_0123456789ab\ncurl localhost:8000/api/v1/benchmark\n</code></pre>"},{"location":"api-reference.html#oauth","title":"OAuth","text":"<p><code>GET /oauth/{provider}/authorize</code> redirects to the provider; the provider returns to <code>/oauth/{provider}/callback</code>. Providers: <code>github</code>, <code>gmail</code>, <code>calendar</code>. Redirect URIs come from <code>GITHUB_OAUTH_REDIRECT_URI</code>, <code>GOOGLE_OAUTH_REDIRECT_URI</code>, <code>GOOGLE_CALENDAR_OAUTH_REDIRECT_URI</code> and must match what you registered with the provider. See connectors.</p>"},{"location":"api-reference.html#webhooks","title":"Webhooks","text":"Endpoint Authentication Failure responses <code>POST /stripe/webhook</code> <code>Stripe-Signature</code> HMAC, checked with <code>STRIPE_WEBHOOK_SECRET</code>, tolerance <code>STRIPE_WEBHOOK_TOLERANCE_SEC</code> 400 invalid signature or payload <code>POST /telegram/webhook</code> Header <code>X-Telegram-Bot-Api-Secret-Token</code> equal to <code>TELEGRAM_WEBHOOK_SECRET</code> 403 invalid secret; 503 secret not configured"},{"location":"api-reference.html#notes","title":"Notes","text":"<ul> <li><code>/approvals</code> lists audit records in <code>PENDING_APPROVAL</code>. Those rows come from the single-action path (<code>POST /actions/run</code>), which   writes the row immediately. In interactive chat runs the approval wait happens before the audit row is written, so a   waiting step appears in <code>GET /chat/execute/{run_id}</code> (status <code>waiting_approval</code>), not in <code>/approvals</code>. Decide chat steps with   <code>POST /chat/execute/{run_id}/respond</code>.</li> <li>Endpoints that read audit data use <code>AUDIT_BACKEND</code> (<code>postgres</code> or <code>jsonl</code>).</li> <li><code>avg_total_ms</code> in <code>/benchmark</code> averages <code>latency.total_ms</code> over all recorded actions.</li> </ul>"},{"location":"architecture.html","title":"Architecture","text":"<p>AgentGate is a modular monolith (Python 3.11, FastAPI, SQLAlchemy 2, Alembic, PostgreSQL/JSONB, Playwright). Decisions behind it: ADR 0001 (modular monolith), ADR 0002 (local-first CLI runtime), and ADR 0003 (embedded guardrail engine).</p>"},{"location":"architecture.html#runtime-chain","title":"Runtime chain","text":"<p>Every guarded action produces this chain; each record carries <code>schema_version</code>, <code>run_id</code>, <code>action_id</code>:</p> <pre><code>flowchart LR\n    A[User prompt] --&gt; B[LLM planner]\n    B --&gt; C[ActionRequest]\n    C --&gt; D[Guardrail]\n    D --&gt;|ALLOW| E[Executor]\n    D --&gt;|NEED_APPROVAL| F[Pause: human]\n    D --&gt;|SANITIZE / ASK_USER| G[Pause: user input]\n    D --&gt;|BLOCK| H[Skip]\n    F --&gt;|approve| E\n    G --&gt;|input| D\n    E --&gt; I[ExecutionResult]\n    H --&gt; I\n    I --&gt; J[AuditEvent]\n    J --&gt; K[ActionTrace]</code></pre>"},{"location":"architecture.html#two-runtimes-one-core","title":"Two runtimes, one core","text":"Local CLI Server Entry <code>agentgate</code> (<code>app/cli</code>) <code>uvicorn app.main:app</code> Process One foreground process Long-running FastAPI service Storage SQLite + <code>guardrail.jsonl</code> in a private data dir PostgreSQL (<code>audit_logs</code>) or JSONL Credentials OS keychain / env / session <code>.env</code> plus <code>oauth_tokens</code> table Approvals Interactive terminal prompt <code>POST /chat/execute/{run_id}/respond</code> or Telegram Guardrail Same engine Same engine <p>The CLI runtime (<code>app/runtime</code>, <code>app/storage/local</code>) swaps in local implementations of audit, tokens, and sanitizing through a runtime context; connectors and the loop are shared. Per ADR 0002 the CLI targets one user, one profile, and one foreground run at a time (a per-profile lock rejects a second active run); the HTTP server is a later adapter over the same application service. Local-first does not mean offline: LLM and connector calls still leave the machine.</p>"},{"location":"architecture.html#reactive-agent-loop","title":"Reactive agent loop","text":"<pre><code>plan -&gt; ( guardrail -&gt; approve / sanitize / execute -&gt; observe -&gt; replan )*\n</code></pre> <ul> <li>Each step is guardrail-checked individually before it runs.</li> <li><code>NEED_APPROVAL</code>, sanitize, and <code>ASK_USER</code> steps pause the run until a response arrives.</li> <li>On failure, or when the plan is exhausted, the LLM re-plans (bounded by <code>AGENT_MAX_REPLAN</code>,   <code>AGENT_MAX_STEPS</code>, <code>AGENT_RUN_TIMEOUT_SEC</code>).</li> <li>Live run state is held in an in-memory registry (<code>RUN_REGISTRY_MAX_SESSIONS</code>). The durable record is the audit store.</li> <li>Run statuses: <code>running</code>, <code>waiting_approval</code>, <code>waiting_input</code>, <code>done</code>, <code>failed</code>, <code>blocked</code>, <code>declined</code>, <code>error</code>, <code>cancelled</code>.</li> </ul>"},{"location":"architecture.html#module-map","title":"Module map","text":"Path Responsibility <code>app/api/v1/</code> Routers: health, runs, actions, audits, approvals, benchmark, chat, oauth, stripe, telegram <code>app/cli/</code>, <code>app/runtime/</code>, <code>app/storage/local/</code>, <code>app/credentials/</code> Local CLI, local runtime, SQLite storage, credential stores <code>app/core/</code> Contracts: <code>action_schema</code>, <code>executor_schema</code>, <code>audit_schema</code>, <code>browser_schema</code>, <code>run_schema</code>, <code>errors</code> <code>app/domains/agent/</code> Planner, agent loop, guarded execution, run registry/service, browser sessions and prototype agent <code>app/domains/guardrail/</code> Decision adapters (<code>agentgate</code>, legacy rules, LLM judge), sensitive-input detection; <code>_vendor/agentgate</code> is the embedded engine <code>app/domains/audit/</code> Audit repositories (PostgreSQL and JSONL) <code>app/domains/approval/</code> Approval schemas, services, repositories <code>app/domains/connector/</code> <code>BaseConnector</code>; <code>filesystem</code>, <code>github</code>, <code>gmail</code>, <code>calendar</code>, <code>telegram</code>, <code>stripe</code> <code>app/domains/oauth/</code> OAuth authorize/callback/refresh and token storage <code>app/domains/browser/</code> Snapshot, selector map, and Playwright executor <code>app/executors/</code> <code>ExecutionRouter</code>, <code>APIExecutor</code>, <code>BrowserExecutor</code> <code>app/llm/</code> Provider client (OpenAI / Anthropic / Gemini dialects), planner parser, accessibility tool <code>app/tracing/</code> <code>LatencyTracker</code>, <code>TraceWriter</code> <code>app/database/</code> Async session, URL/TLS handling, ORM models, Alembic migrations <code>fe/</code> Static browser demo <code>app/demo/</code> Browser demo scripts and artifact saving"},{"location":"architecture.html#decision-routing","title":"Decision routing","text":"<p><code>ExecutionRouter.route(action, decision)</code>:</p> <ul> <li><code>BLOCK</code> -&gt; not executed, status <code>BLOCKED</code></li> <li><code>NEED_APPROVAL</code> -&gt; not executed, status <code>PENDING_APPROVAL</code></li> <li>otherwise -&gt; <code>BrowserExecutor</code> if <code>target_system == \"browser\"</code> or <code>action_type</code> starts with <code>BROWSER_</code>,   else <code>APIExecutor</code></li> </ul> <p><code>APIExecutor</code> picks a connector by <code>target_system</code> and requires <code>payload.action</code>. <code>BrowserExecutor</code> drives the same Playwright pipeline used by multi-step browser plans.</p>"},{"location":"architecture.html#browser-pipeline","title":"Browser pipeline","text":"<p>The planner sees short element IDs only; selectors stay server-side.</p> <pre><code>ARIA snapshot -&gt; semantic elements (+ risk_hint) -&gt; DOM inspection -&gt; matcher (element_id)\n              -&gt; locator candidates -&gt; ranker (selector_map) -&gt; executor (click / fill / ...)\n</code></pre> <p>Consecutive <code>BROWSER_*</code> steps sharing a URL run as a batch in one Playwright session. By default a batch writes one combined audit row; with <code>ATOMIC_BROWSER_AUDIT=true</code> each step gets its own row (see audit-design).</p>"},{"location":"architecture.html#integrations-as-channels","title":"Integrations as channels","text":"<p>Telegram can start runs (webhook -&gt; shared <code>start_agent_run()</code> -&gt; the same loop) and receives approval callbacks. Stripe events are reconciled through a signed webhook. See connectors.</p>"},{"location":"audit-design.html","title":"Audit design: current vs. target","text":""},{"location":"audit-design.html#principles-both-designs","title":"Principles (both designs)","text":"<ul> <li>Every action is audited: allowed, blocked, declined, failed, and pending ones.</li> <li>Audit records are immutable.</li> <li>Only sensitive entity kinds are stored. <code>ActionRequest.payload</code> is excluded from serialized audit records.</li> </ul>"},{"location":"audit-design.html#current-implementation-action-sourced","title":"Current implementation: action-sourced","text":"<p>One row per action, written once when the outcome is known.</p> Aspect Detail Server table <code>audit_logs</code> (migration <code>0001</code>): <code>audit_id</code> PK, <code>action_id</code> unique, <code>run_id</code> indexed, JSONB request/decision/execution Immutability Trigger <code>trg_audit_logs_immutable</code> rejects UPDATE and DELETE Backends <code>AuditRepositoryDB</code> (postgres) and <code>AuditRepository</code> (JSONL), same async interface: <code>write</code>, <code>latest</code>, <code>list</code>, <code>by_run</code>, <code>by_action</code>. <code>get_audit_repository()</code> picks by <code>AUDIT_BACKEND</code> (or the CLI runtime's SQLite store) Guardrail journal Separate <code>guardrail.jsonl</code>: one entry per evaluation (<code>guard_...</code> ID), including re-evaluations after input or redaction. Final audit rows reference it through <code>guardrail_audit_id</code> Traces <code>actions.jsonl</code> (<code>ActionTrace</code>), a model-ready export separate from audit"},{"location":"audit-design.html#when-rows-are-written","title":"When rows are written","text":"Path Row written Row content Chat run, <code>ALLOW</code> After execution <code>SUCCESS</code> / <code>FAILED</code> Chat run, <code>NEED_APPROVAL</code> approved After execution Decision is <code>ALLOW</code> with <code>initial_decision=NEED_APPROVAL</code>, <code>approval_decision=\"approved\"</code> Chat run, declined Immediately <code>SKIPPED</code>, \"declined by user\", <code>approval_decision=\"declined\"</code> Chat run, <code>BLOCK</code> Immediately <code>BLOCKED</code> / <code>SKIPPED</code> Chat run, timeout waiting None found The loop returns without a row <code>POST /actions/run</code>, <code>NEED_APPROVAL</code> Immediately <code>PENDING_APPROVAL</code> snapshot (nothing resumes it) Browser batch (default) After the batch One combined row for consecutive browser steps sharing a URL Browser batch, <code>ATOMIC_BROWSER_AUDIT=true</code> Per step One row per step (own <code>action_id</code>, shared <code>run_id</code>); after a mid-batch failure the remaining steps are <code>SKIPPED</code> <p>Reading: the approval outcome is captured on the single row, but the timeline (when approval was requested, when it was decided, who decided) is not stored as separate records.</p>"},{"location":"audit-design.html#consequences","title":"Consequences","text":"<ul> <li><code>action_id</code> uniqueness means an action can have only one row; a <code>PENDING_APPROVAL</code> snapshot from <code>/actions/run</code>   cannot later be superseded by a second row for the same action.</li> <li>Intermediate states (sanitized preview, clarification exchange, approval request time) are visible only in the   live run state and the guardrail journal, not in <code>audit_logs</code>.</li> <li>For declined, blocked, and skipped steps the loop logs a warning and continues if the audit write fails,   so an audit failure there does not stop the run.</li> </ul>"},{"location":"audit-design.html#target-event-sourced-spec-v4-agentgate_sprint2_audit_schema_v4_iddocx","title":"Target: event-sourced (spec v4, <code>AgentGate_Sprint2_Audit_Schema_v4_ID.docx</code>)","text":"<p>An append-only <code>audit_events</code> table where each lifecycle step is its own event and state is derived by replaying events ordered by <code>sequence_no</code>.</p> Category Events Session <code>SESSION_STARTED</code>, <code>SESSION_ENDED</code>, <code>PROMPT_SUBMITTED</code> Agent / guardrail <code>ACTION_CREATED</code>, <code>GUARDRAIL_EVALUATED</code>, <code>PAYLOAD_SANITIZED</code>, <code>ACTION_BLOCKED</code> Human interaction <code>USER_CLARIFICATION_REQUESTED</code>, <code>USER_CLARIFICATION_RECEIVED</code>, <code>APPROVAL_REQUESTED</code>, <code>APPROVAL_DECIDED</code>, <code>ACTION_REJECTED</code> Execution <code>EXECUTION_STARTED</code>, <code>EXECUTION_FINISHED</code>, <code>EXECUTION_FAILED</code>, <code>EXECUTION_SKIPPED</code> <p>Envelope: <code>audit_event_id</code>, <code>session_id</code>, <code>action_id</code>, <code>event_type</code>, <code>timestamp</code>, <code>sequence_no</code>, <code>written_by</code>, <code>domain</code>, <code>decision</code> (denormalized and indexed to keep Sprint 1 query patterns).</p> <p>Derived states: <code>CREATED</code>, <code>UNDER_REVIEW</code>, <code>SANITIZED</code>, <code>AWAITING_USER</code>, <code>AWAITING_APPROVAL</code>, <code>APPROVED</code>, <code>REJECTED</code>, <code>BLOCKED</code>, <code>EXECUTING</code>, <code>SUCCESS</code>, <code>FAILED</code>.</p>"},{"location":"audit-design.html#gap-summary","title":"Gap summary","text":"Topic Spec v4 Code today Table <code>audit_events</code> (many rows per action) <code>audit_logs</code> (one row per action) Ordering <code>sequence_no</code> per session <code>created_at</code> Approval history <code>APPROVAL_REQUESTED</code> then <code>APPROVAL_DECIDED</code> Outcome on the final row (<code>initial_decision</code>, <code>approval_decision</code>) Skipped actions Explicit <code>EXECUTION_SKIPPED</code> event Status <code>SKIPPED</code> / <code>BLOCKED</code> on the row Sanitize / clarification history Separate events Guardrail journal entries per evaluation Session events Defined Not present <code>decision</code>, <code>domain</code> columns Denormalized, indexed Inside JSONB, not indexed columns Browser granularity per action per batch by default; per step with <code>ATOMIC_BROWSER_AUDIT</code> <p>The guardrail journal plus <code>initial_decision</code> / <code>approval_decision</code> narrow the gap, but they do not give an ordered, queryable event stream. Closing it requires a new migration and repository. The proposal is captured in ADR 0004.</p>"},{"location":"cli.html","title":"Local CLI","text":"<p>AgentGate runs the planner, guardrail, approvals, and connectors in one foreground process. No FastAPI server, Docker, PostgreSQL, or Redis is required. Local-first is not offline: prompts and relevant observations go to the configured external LLM, and provider integrations make outbound API calls.</p>"},{"location":"cli.html#install","title":"Install","text":"<p>From this checkout, using Python 3.11 or newer:</p> <pre><code>pipx install .\nagentgate --help\nagentgate init\nagentgate doctor\n</code></pre> <p>An alternative is installing a built wheel with pipx. This package is not assumed to be published on PyPI. <code>python -m app</code> also provides the installed CLI. For server development and the full test dependencies:</p> <pre><code>uv sync --extra dev --extra server --extra browser --extra stripe\n</code></pre> <p><code>init</code> selects the LLM dialect, endpoint/model, timezone, explicitly allowed workspace, and credential store. Hidden API-key input goes to the OS keychain, not TOML. Repeated init preserves existing config. Headless setup:</p> <pre><code>agentgate init --non-interactive --workspace /absolute/path/to/project --credential-store env\n</code></pre> <p>Supply <code>LLM_API_KEY</code> securely through your calling environment. There are no secret-valued command-line flags and no implicit repository <code>.env</code> loading.</p> <p>The default guardrail requires Ollama and a detector model:</p> <pre><code>ollama pull qwen2.5:7b\n# In the shell running Ollama:\nOLLAMA_NUM_PARALLEL=6 ollama serve\nagentgate doctor\n</code></pre> <p>If Ollama already runs as a service, configure its parallelism in that service. On PowerShell, use <code>$env:OLLAMA_NUM_PARALLEL = \"6\"</code> before <code>ollama serve</code>. Supported shell overrides are <code>OLLAMA_HOST</code>, <code>AGENTGATE_LLM_DETECTOR_MODEL</code>, <code>AGENTGATE_LLM_DETECTOR_TIMEOUT</code>, and <code>AGENTGATE_DETECTOR_ARCHITECTURE</code> (<code>six</code> or experimental <code>unified</code>). These are independent of the planner configuration. <code>GUARDRAIL_BACKEND=legacy</code> explicitly restores the earlier guardrail. See guardrail integration for behavior and audit details.</p>"},{"location":"cli.html#commands-and-interaction","title":"Commands and interaction","text":"Command Purpose <code>agentgate init</code> Guided setup and local schema initialization <code>agentgate doctor</code> Credential-presence, workspace, storage, browser, and detector readiness checks <code>agentgate connect llm</code> Store an LLM key through hidden input <code>agentgate connect github</code> Store a user-supplied GitHub token <code>agentgate connect gmail</code> Google Desktop OAuth for Gmail read access <code>agentgate connect calendar</code> Google Desktop OAuth for Calendar event access <code>agentgate connect stripe</code> Store a supplied Stripe test-mode key <code>agentgate disconnect &lt;provider&gt;</code> Remove credentials; revoke Google OAuth first <code>agentgate setup browser</code> Explicitly download Chromium and enable browser execution <code>agentgate run \"&lt;task&gt;\"</code> Execute a foreground guarded run <code>agentgate history --limit 20</code> Read previous run summaries <code>agentgate show &lt;run-id&gt;</code> Read saved steps, events, and audit <code>agentgate payments sync</code> Retrieve tracked Stripe session/refund status <p>Common options, before or after the command: <code>--config /path/config.toml</code>, <code>--credential-store keyring|env|session</code>, and <code>--json</code>. <code>run</code> also accepts <code>--workspace</code>; <code>run</code> and <code>init</code> accept <code>--non-interactive</code>.</p> <pre><code>agentgate connect calendar\nagentgate run \"Create a 30-minute calendar event tomorrow at 10am called Demo\"\n</code></pre> <p>Missing information prompts for structured clarification before approval. Approval shows the action, target, payload, risk, and policy reasons. Only <code>y</code>/<code>yes</code> approves; other responses decline. Secret/sanitize fields use hidden input. Each replanned write independently passes policy and approval. Clarification is not approval of a Calendar or financial write. One active run is allowed per data profile.</p> <p>Ctrl+C cancels and closes browser resources, but does not undo remote effects. Abandoned runs are marked interrupted on the next runtime startup. History is not resumption. No daemon, background execution, cross-process responses, <code>serve</code>, or automatic <code>resume</code> is implemented.</p> <pre><code>agentgate run \"Read README.md\" --workspace /absolute/path/to/project --json --non-interactive\n</code></pre> <p>Run JSON output is sanitized NDJSON with schema version, run ID, sequence, timestamp, type, and data. Diagnostics/logs use stderr. Required approval/input fails closed with exit code 3; there is no <code>--yes</code> bypass. Rerunning creates a new run.</p> Exit code Meaning 0 Successful operation 1 Failed/blocked/declined run, failed diagnostics, unresolved reconciliation 2 Invalid usage/configuration or local storage setup 3 Terminal interaction required 130 User interruption"},{"location":"cli.html#local-configuration-and-credentials","title":"Local configuration and credentials","text":"<p>Linux defaults: <code>~/.config/agentgate/config.toml</code> and <code>~/.local/share/agentgate/state.sqlite3</code>, honoring XDG variables. Other platforms use standard per-user locations. Override <code>AGENTGATE_CONFIG_DIR</code> and <code>AGENTGATE_DATA_DIR</code> to isolate a profile. Package/repository paths are not data paths.</p> <p>SQLite stores sanitized history, safe audit/traces, OAuth metadata, payment status with checked-at timestamps, and execution intents. Tokens/keys live outside SQLite. The embedded engine additionally appends each evaluation to <code>guardrail.jsonl</code> in the private data directory before returning a verdict. Final action audits in SQLite reference the evaluation's <code>guardrail_audit_id</code>. Redacted content is re-evaluated and shown for approval; it is not treated as missing secret input. Audit is append-only through SQLite triggers, not tamper-proof against the owner. Do not put credentials in prompts. Masking covers known credentials and recognized secret fields/formats, not every arbitrary string that a user might consider secret. Known credential values are also masked from outgoing LLM message context.</p> <p>Credential modes:</p> <ul> <li><code>keyring</code>: supported secure OS keychain backends only. Missing/locked keychains   fail explicitly, with no silent plaintext fallback.</li> <li><code>env</code>: explicitly read <code>LLM_API_KEY</code>, <code>GITHUB_TOKEN</code>, <code>GMAIL_ACCESS_TOKEN</code>,   <code>GOOGLE_CALENDAR_ACCESS_TOKEN</code>, <code>STRIPE_SECRET_KEY</code>, and   <code>GOOGLE_OAUTH_CLIENT_SECRET</code>. Connect checks presence; disconnect tells you to   unset the variable. Environment credentials are never modified or written to files.</li> <li><code>session</code>: run collects hidden credentials for that process only. Connect cannot   persist session credentials.</li> </ul> <p>Config fields: <code>llm_type</code>, <code>llm_url</code>, <code>llm_model</code>, <code>timezone</code>, <code>workspace</code>, <code>credential_store</code>, <code>browser_enabled</code>, <code>google_client_id</code>, <code>stripe_success_url</code>, <code>stripe_cancel_url</code>, and the <code>stripe_price_map</code> table. Unknown fields are rejected. Project config is loaded only with explicit <code>--config</code>. It cannot lower mandatory guardrail rules or add filesystem roots.</p> <p>Non-secret environment overrides: <code>LLM_TYPE</code>, <code>LLM_URL</code>, <code>LLM_MODEL</code>, <code>GOOGLE_OAUTH_CLIENT_ID</code>, <code>STRIPE_SUCCESS_URL</code>, <code>STRIPE_CANCEL_URL</code>, and <code>STRIPE_PRICE_MAP</code> (JSON object). Timeout/step limits and optional guardrail-model settings retain their existing environment names. Unrelated server variables are ignored. The selected credential mode does not silently change because a variable exists.</p>"},{"location":"cli.html#optional-integrations","title":"Optional integrations","text":""},{"location":"cli.html#browser","title":"Browser","text":"<p>Install extras from the source checkout or an extra-enabled release wheel:</p> <pre><code>pipx install --force '.[browser]'\nagentgate setup browser\nagentgate doctor\n</code></pre> <p>Include all needed extras when reinstalling, for example <code>.[browser,stripe]</code>. Setup explicitly downloads Chromium. OS browser libraries may also be needed; AgentGate does not install system packages or request elevated access. Browser state is ephemeral across runs. Screenshots go only under the profile artifact directory, never to a model-selected path. Images can contain private page content; inspect artifacts before sharing.</p>"},{"location":"cli.html#google-and-github","title":"Google and GitHub","text":"<p>Create a Google OAuth Desktop app registration and enable the Gmail/Calendar APIs. Connect prompts for a Desktop client ID and, when required, its user-supplied secret. The ID is non-secret config; the secret and OAuth tokens are stored in the OS keychain. The system browser opens, a short-lived <code>127.0.0.1</code> listener receives the state-checked PKCE callback, then closes. No persistent API server or public callback is required. See Google Desktop OAuth.</p> <p>Gmail requests read-only scope; Calendar requests event scope. OAuth refresh updates the secret store. Environment mode uses supplied access tokens without persistent OAuth refresh. Google revocation can also invalidate the other Google integration; reconnect it if necessary. GitHub uses a supplied token for repository metadata. No confidential web-app secret is bundled with the CLI.</p>"},{"location":"cli.html#stripe","title":"Stripe","text":"<pre><code>pipx install --force '.[stripe]'\nagentgate connect stripe\n</code></pre> <p>Configure valid landing URLs and an allowlisted test catalog in TOML. No nonexistent localhost landing page is assumed. Place scalar fields before TOML table headers:</p> <pre><code>stripe_success_url = \"https://your-merchant.example/success\"\nstripe_cancel_url = \"https://your-merchant.example/cancel\"\n\n[stripe_price_map]\ndemo = \"price_YOUR_TEST_PRICE_ID\"\n</code></pre> <p>Plans choose catalog keys/quantities, not arbitrary prices or URLs. Financial writes require test-mode keys and approval. Checkout URLs appear in terminal results; open one in the system browser and enter card details on Stripe's hosted page, not in AgentGate. Completion/redirects are not proof of payment.</p> <p>Sync explicitly refreshes known sessions/refunds. Nothing updates while the CLI is closed. The journal commits identity before effects. Equivalent completed writes in the same run reuse results; unknown writes are blocked rather than replayed. Known remote IDs remain recoverable even after payment-row persistence failure. Unknown operations without IDs require manual provider reconciliation and remain blocked; no unsafe clear-and-retry command exists. Reliable unattended fulfillment, async payment notifications, and signed inbound webhooks belong to the server phase. See Stripe fulfillment requirements.</p>"},{"location":"cli.html#development-boundary","title":"Development boundary","text":"<p><code>AgentService</code> uses task-scoped <code>LocalRuntime</code> dependencies. Existing HTTP routes retain their defaults; install the <code>server</code> extra to develop them. PostgreSQL migration history is unchanged; SQLite uses its own versioned schema. See ADR 0002.</p>"},{"location":"configuration.html","title":"Configuration","text":"<p>Server settings load from environment variables and <code>.env</code> through <code>app/config/settings.py</code> (pydantic-settings). <code>APP_ENV</code> selects a subclass with different defaults. Templates: <code>.env.example</code>, <code>.env.development</code>, <code>.env.staging</code>. The CLI does not read <code>.env</code>; it uses <code>config.toml</code> and the credential store (cli.md).</p> <p>Values below are the defaults in code. Where a template overrides one, it is noted.</p>"},{"location":"configuration.html#core","title":"Core","text":"Variable Default Purpose <code>APP_ENV</code> <code>development</code> <code>development</code>, <code>staging</code>, <code>production</code> <code>DEBUG</code> <code>False</code> Debug mode (True in development profile) <code>LOG_LEVEL</code> <code>INFO</code> <code>DEBUG</code>..<code>ERROR</code> (DEBUG in development profile). Structured JSON logs"},{"location":"configuration.html#database-and-audit","title":"Database and audit","text":"Variable Default Purpose <code>DATABASE_URL</code> <code>postgresql+psycopg://agentgate:agentgate@localhost:5432/agentgate</code> Must start with <code>postgresql</code> or <code>sqlite</code>. <code>.env.example</code> uses <code>+asyncpg</code> <code>DATABASE_POOL_SIZE</code> 10 (1-100) Pool size <code>DATABASE_MAX_OVERFLOW</code> 20 Extra connections <code>DATABASE_SSL_MODE</code> <code>auto</code> <code>auto</code> verifies TLS for Neon / <code>sslmode=require</code> URLs; <code>require</code>; <code>disable</code> (asyncpg driver only) <code>AUDIT_BACKEND</code> <code>postgres</code> <code>postgres</code> or <code>jsonl</code>. <code>.env.development</code> sets <code>jsonl</code> <code>ATOMIC_BROWSER_AUDIT</code> <code>False</code> One audit row per browser step instead of one per batch <code>AUDIT_LOG_PATH</code> <code>artifacts/audit/events.jsonl</code> JSONL audit file <code>TRACE_LOG_PATH</code> <code>artifacts/traces/actions.jsonl</code> Action traces <code>GUARDRAIL_AUDIT_PATH</code> <code>artifacts/audit/guardrail.jsonl</code> Guardrail evaluation journal <code>AUDIT_RETENTION_DAYS</code> 7 Staging 30, production 365 <code>TRACE_RETENTION_DAYS</code> 7 Staging 14, production 90 <code>SCREENSHOT_RETENTION_DAYS</code> 7 Production 30 <code>DATA_DIR</code> <code>./data</code> Local data (screenshots under <code>data/browser/screenshots/</code>) <p>Retention values are configuration only. No cleanup job was found in the code reviewed; see limitations.</p>"},{"location":"configuration.html#llm-planner","title":"LLM planner","text":"Variable Default Purpose <code>LLM_TYPE</code> <code>openai</code> <code>openai</code> (compatible), <code>anthropic</code>, or <code>gemini</code> <code>LLM_URL</code> <code>https://openrouter.ai/api/v1/chat/completions</code> Full chat endpoint <code>LLM_MODEL</code> <code>openrouter/free</code> Model id <code>LLM_API_KEY</code> empty <code>Bearer</code> (openai) or <code>x-api-key</code> (anthropic) <code>LLM_TIMEOUT</code> 60 Seconds <code>LLM_MAX_TOKENS</code> 4096 (256-128000) Response cap <code>LLM_TOOLS_ENABLED</code> True Tool calling <code>LLM_MAX_TOOL_ITERATIONS</code> 5 (1-10) Tool-call rounds <code>LLM_PLUGINS</code> empty Provider-specific request plugins; leave empty if unsupported"},{"location":"configuration.html#planner-validation","title":"Planner validation","text":"<p>Each takes a comma-separated list. Anything outside these lists is rejected or blocked.</p> Variable Default <code>ALLOWED_ACTION_TYPES</code> <code>BROWSER_OPEN, BROWSER_CLICK, BROWSER_TYPE, BROWSER_SCROLL, BROWSER_SCREENSHOT, BROWSER_SUBMIT, BROWSER_SELECT, API_CALL, FILE_READ</code> <code>ALLOWED_TARGET_SYSTEMS</code> <code>browser, calendar, gmail, github, local_file, stripe, telegram</code> <code>ALLOWED_DOMAINS</code> <code>browser, productivity, code_protection, booking, filesystem</code> <code>ALLOWED_RISK_HINTS</code> <code>unknown, external_send, file_read, destructive, unauthorized, data_exfiltration, payment, refund, bulk_action</code> <code>INTERACTIVE_BROWSER_ACTIONS</code> <code>BROWSER_CLICK, TYPE, SCROLL, SCREENSHOT, SUBMIT, SELECT</code> <code>DOMAIN_BY_TARGET_SYSTEM</code> calendar/gmail/telegram -&gt; productivity; github -&gt; code_protection; local_file -&gt; filesystem; stripe -&gt; booking; browser -&gt; browser <code>DEFAULT_DOMAIN</code> <code>productivity</code> <p><code>DOMAIN_BY_TARGET_SYSTEM</code> stops the planner from lowering risk by omitting or misreporting <code>domain</code>.</p>"},{"location":"configuration.html#guardrail","title":"Guardrail","text":"Variable Default Purpose <code>GUARDRAIL_BACKEND</code> <code>agentgate</code> <code>agentgate</code> (embedded engine) or <code>legacy</code> (explicit rollback, no auto fallback) <code>OLLAMA_HOST</code> <code>http://localhost:11434</code> Detector endpoint. Remote hosts require HTTPS; <code>host.docker.internal</code> is allowed over HTTP <code>AGENTGATE_LLM_DETECTOR_MODEL</code> <code>qwen2.5:7b</code> Detector model <code>AGENTGATE_LLM_DETECTOR_TIMEOUT</code> 30 Seconds per request, no retries <code>AGENTGATE_DETECTOR_ARCHITECTURE</code> <code>six</code> <code>six</code> or experimental <code>unified</code> <code>GUARDRAIL_BLOCK_HINTS</code> <code>destructive, unauthorized, data_exfiltration</code> Hints that map to <code>BLOCK</code> <code>GUARDRAIL_NEED_APPROVAL_HINTS</code> <code>external_send, payment, bulk_action, refund</code> Hints that map to <code>NEED_APPROVAL</code> <code>GUARDRAIL_ASK_USER_HINTS</code> <code>ambiguous_target, missing_target, clarification_needed</code> Hints that map to <code>ASK_USER</code> <code>GUARDRAIL_LLM_ENABLED</code> <code>True</code> in code, <code>False</code> in <code>.env.example</code> Legacy backend only: second-opinion LLM judge <code>GUARDRAIL_MODEL</code> empty Legacy judge model; empty falls back to <code>LLM_MODEL</code> <p>Run Ollama with <code>OLLAMA_NUM_PARALLEL=6</code> so the six detectors can overlap. The CLI reads <code>OLLAMA_HOST</code>, <code>AGENTGATE_LLM_DETECTOR_MODEL</code>, <code>AGENTGATE_LLM_DETECTOR_TIMEOUT</code>, <code>AGENTGATE_DETECTOR_ARCHITECTURE</code> from the shell only.</p>"},{"location":"configuration.html#agent-loop","title":"Agent loop","text":"Variable Default Purpose <code>AGENT_MAX_STEPS</code> 12 (1-100) Steps per run, including replanned steps <code>AGENT_MAX_REPLAN</code> 4 (0-20) Replan calls <code>AGENT_WAIT_RESPONSE_TIMEOUT_SEC</code> 600 Wait for approval/input <code>AGENT_RUN_TIMEOUT_SEC</code> 900 Whole-run timeout <code>SSE_HEARTBEAT_SEC</code> 15 SSE keep-alive <code>RUN_REGISTRY_MAX_SESSIONS</code> 500 In-memory run cap"},{"location":"configuration.html#filesystem-and-browser","title":"Filesystem and browser","text":"Variable Default Purpose <code>LOCAL_FILE_ROOT</code> <code>demo_data</code> Root for file reads <code>ALLOWED_FILESYSTEM_PATHS</code> <code>/tmp/agentgate</code> Extra absolute allowed dirs (comma-separated or JSON list) <code>PLAYWRIGHT_HEADLESS</code> True <code>.env.development</code> sets False <code>PLAYWRIGHT_MAX_ELEMENTS</code> 50 (10-500) Elements per snapshot <code>BROWSER_MAX_CONCURRENT_SESSIONS</code> 10 Staging 25, production 50 <code>BROWSER_WAIT_UNTIL</code> <code>domcontentloaded</code> <code>commit</code>, <code>domcontentloaded</code>, <code>load</code>, <code>networkidle</code> <code>BROWSER_TIMEOUT_MS</code> 15000 (1000-60000) Navigation timeout <code>BROWSER_SETTLE_MS</code> 2000 (0-10000) Wait after load <code>BROWSER_USER_AGENT</code> empty Empty uses the built-in Chrome profile"},{"location":"configuration.html#connectors","title":"Connectors","text":"Group Variables GitHub <code>GITHUB_OAUTH_CLIENT_ID</code>, <code>GITHUB_OAUTH_CLIENT_SECRET</code>, <code>GITHUB_OAUTH_REDIRECT_URI</code>; fallback token <code>GITHUB_TOKEN</code> Google <code>GOOGLE_OAUTH_CLIENT_ID</code>, <code>GOOGLE_OAUTH_CLIENT_SECRET</code>, <code>GOOGLE_OAUTH_REDIRECT_URI</code> (Gmail), <code>GOOGLE_CALENDAR_OAUTH_REDIRECT_URI</code>; fallback tokens <code>GMAIL_ACCESS_TOKEN</code>, <code>GOOGLE_CALENDAR_ACCESS_TOKEN</code> Calendar <code>CALENDAR_DEFAULT_TIMEZONE</code> (default <code>Asia/Jakarta</code>) Telegram <code>TELEGRAM_BOT_TOKEN</code>, <code>TELEGRAM_WEBHOOK_SECRET</code>, <code>TELEGRAM_API_BASE</code> Stripe <code>STRIPE_SECRET_KEY</code>, <code>STRIPE_WEBHOOK_SECRET</code>, <code>STRIPE_SUCCESS_URL</code>, <code>STRIPE_CANCEL_URL</code>, <code>STRIPE_PRICE_MAP</code> (JSON), <code>STRIPE_MAX_QUANTITY</code> (20), <code>STRIPE_WEBHOOK_TOLERANCE_SEC</code> (300) Other <code>XENDIT_API_KEY</code> is defined in settings; no code path using it was found <p>Static tokens (<code>GITHUB_TOKEN</code>, etc.) are a fallback used when no OAuth token is stored.</p>"},{"location":"configuration.html#environment-profiles","title":"Environment profiles","text":"development staging production <code>DEBUG</code> / <code>LOG_LEVEL</code> True / DEBUG False / INFO False / INFO Audit / trace retention (days) 7 / 7 30 / 14 365 / 90 Screenshot retention (days) 7 7 30 Max browser sessions 10 25 50 <p>Profile classes set only the values above. Other differences come from the <code>.env.*</code> templates (for example <code>PLAYWRIGHT_HEADLESS</code> and <code>AUDIT_BACKEND</code>).</p> <p>Never commit real secrets. <code>.env.development</code> and <code>.env.staging</code> are tracked (only <code>.env.production</code> is ignored via the commented rule), so keep real values out of them; use a secret manager in production (ADR 0001).</p>"},{"location":"connectors.html","title":"Connectors","text":"<p>Every connector implements <code>BaseConnector.execute(action, payload) -&gt; ExecutionResult</code> (<code>app/domains/connector/base.py</code>), returns normalized <code>ConnectorError</code> codes on failure, and is reached only after a guardrail verdict. The <code>payload.action</code> field selects the operation; <code>target_system</code> selects the connector.</p>"},{"location":"connectors.html#summary","title":"Summary","text":"Connector <code>target_system</code> Operations Auth Guardrail floor Local files <code>local_file</code> <code>read</code> none (path allowlist) none GitHub <code>github</code> <code>repo_metadata</code> OAuth (<code>repo</code> scope) or <code>GITHUB_TOKEN</code> none; domain <code>code_protection</code> is HIGH Gmail <code>gmail</code> <code>list_messages</code> OAuth <code>gmail.readonly</code> or <code>GMAIL_ACCESS_TOKEN</code> none Calendar <code>calendar</code> <code>list_events</code>, <code>create_event</code> OAuth <code>calendar</code> (full) or token create: <code>external_send</code> Telegram <code>telegram</code> <code>send_message</code>, <code>answer_callback_query</code>, <code>edit_message_reply_markup</code> Bot token <code>external_send</code> Stripe <code>stripe</code> create/retrieve/expire checkout session, create/retrieve refund Secret key <code>payment</code> / <code>refund</code> Browser <code>browser</code> open, snapshot, click, type, select, submit, scroll, screenshot none submit needs approval <p>Errors map to <code>AUTH</code>, <code>PERMISSION</code>, <code>RATE_LIMIT</code>, <code>TIMEOUT</code>, <code>VALIDATION</code>, <code>UNAVAILABLE</code>, <code>NOT_FOUND</code>, <code>UNKNOWN</code>. Timeouts and unavailable services are marked <code>retryable</code>.</p>"},{"location":"connectors.html#local-files","title":"Local files","text":"<ul> <li>Reads only <code>read</code>, returning up to 500 characters as <code>content_preview</code>.</li> <li>The path is resolved (symlinks and <code>..</code> collapsed) and must sit inside <code>LOCAL_FILE_ROOT</code> or one of   <code>ALLOWED_FILESYSTEM_PATHS</code>. Otherwise the result is <code>FAILED</code> with a validation error.</li> <li>Payload: <code>{\"action\":\"read\",\"path\":\"sample.txt\"}</code>.</li> <li>Setup: put files under <code>LOCAL_FILE_ROOT</code> (default <code>demo_data/</code>).</li> </ul>"},{"location":"connectors.html#github","title":"GitHub","text":"<ul> <li>Operation <code>repo_metadata</code>, payload <code>{\"action\":\"repo_metadata\",\"owner\":\"...\",\"repo\":\"...\"}</code> (or a combined repo reference).</li> <li>Returns id, full name, privacy, default branch, URL, description, stars, forks, open issues.</li> <li>Auth options:</li> <li>OAuth (server): register a GitHub OAuth app; set <code>GITHUB_OAUTH_CLIENT_ID</code>, <code>GITHUB_OAUTH_CLIENT_SECRET</code>,      <code>GITHUB_OAUTH_REDIRECT_URI</code> (default <code>http://localhost:8000/api/v1/oauth/github/callback</code>), open      <code>/api/v1/oauth/github/authorize</code>. Scope requested: <code>repo</code>.</li> <li>Token: <code>GITHUB_TOKEN</code> (server) or <code>agentgate connect github</code> (CLI).</li> <li>Without a token, public repositories can still be read subject to GitHub's unauthenticated rate limits.</li> </ul>"},{"location":"connectors.html#gmail","title":"Gmail","text":"<ul> <li>Operation <code>list_messages</code>, payload <code>{\"action\":\"list_messages\",\"query\":\"...\",\"max_results\":10}</code>.</li> <li>Returns message IDs and a size estimate. Read-only: it cannot send or modify mail.</li> <li>Setup (Google Cloud Console):</li> <li>Create a project and enable the Gmail API.</li> <li>Configure the OAuth consent screen (add yourself as a test user while unverified).</li> <li>Create an OAuth client; add the redirect URI <code>http://localhost:8000/api/v1/oauth/gmail/callback</code>.</li> <li>Set <code>GOOGLE_OAUTH_CLIENT_ID</code>, <code>GOOGLE_OAUTH_CLIENT_SECRET</code>, <code>GOOGLE_OAUTH_REDIRECT_URI</code>.</li> <li>Open <code>/api/v1/oauth/gmail/authorize</code> (server) or run <code>agentgate connect gmail</code> (CLI, Desktop OAuth).</li> <li>Scope: <code>https://www.googleapis.com/auth/gmail.readonly</code>. Offline access with <code>prompt=consent</code> is requested so a   refresh token is issued.</li> </ul>"},{"location":"connectors.html#google-calendar","title":"Google Calendar","text":"<ul> <li>Operations: <code>list_events</code> (optional <code>max_results</code>, default 10) and <code>create_event</code>.   <code>create_event</code> payload: <code>summary</code>, <code>start</code>, <code>end</code> (required), optional <code>timezone</code>, <code>description</code>, <code>location</code>,   <code>calendar_id</code> (default <code>primary</code>). The payload is validated first; <code>summary</code> and <code>description</code> can be redacted.</li> <li>Timezone default: <code>CALENDAR_DEFAULT_TIMEZONE</code> (<code>Asia/Jakarta</code>).</li> <li>Setup: enable the Google Calendar API on the same OAuth client and also register <code>GOOGLE_CALENDAR_OAUTH_REDIRECT_URI</code> (<code>.../oauth/calendar/callback</code>) as an authorized redirect URI.   Then open <code>/api/v1/oauth/calendar/authorize</code> or run <code>agentgate connect calendar</code>.</li> <li>Scope: <code>https://www.googleapis.com/auth/calendar</code> (full read/write). Grant only to accounts you trust the agent with.</li> </ul>"},{"location":"connectors.html#telegram","title":"Telegram","text":"<ul> <li>Operations: send a message, answer a callback query, edit a reply markup.</li> <li><code>recipient</code> is a human-readable name resolved against the contact registry (<code>telegram_contacts</code>) into a   numeric <code>chat_id</code> before approval; the connector never guesses from a name. A numeric <code>chat_id</code> is used   directly only if the user gave one.</li> <li>Outbound sends work from both the CLI and the server. Inbound (the webhook that starts runs and receives   approval callbacks) is server-only; ADR 0002 keeps it out of the local CLI release.</li> <li>Inbound: the webhook (<code>/telegram/webhook</code>) starts runs and receives approval button callbacks. It requires   <code>TELEGRAM_WEBHOOK_SECRET</code>; register it with <code>scripts/set_telegram_webhook.py</code>.</li> <li>Full guide: integrations/telegram.md.</li> </ul>"},{"location":"connectors.html#stripe","title":"Stripe","text":"<ul> <li>Small, deliberate surface: hosted Checkout Sessions and refunds only. Prices come from the server-side   allowlist <code>STRIPE_PRICE_MAP</code>; the planner selects a catalog key, never a raw price ID or amount.</li> <li>Approval is required for financial writes. Use test mode keys first.</li> <li>Webhook <code>/stripe/webhook</code> verifies the <code>Stripe-Signature</code> HMAC and deduplicates by event ID (server).</li> <li>CLI: <code>agentgate connect stripe</code> takes a user-supplied test-mode key through hidden input (not Stripe Connect   OAuth). Checkout opens in the user's browser so card details never touch AgentGate. There is no webhook locally:   <code>agentgate payments sync</code> retrieves tracked sessions/refunds and stores the provider status with a checked-at   time. A redirect is never proof of payment, and nothing updates while the CLI is closed. Configure real   <code>stripe_success_url</code> / <code>stripe_cancel_url</code>; there are no local landing pages.</li> <li>Retries reuse the persisted operation and idempotency key; unknown outcomes are reconciled before another   financial attempt is allowed.</li> <li>Full guide: integrations/stripe.md.</li> </ul>"},{"location":"connectors.html#browser","title":"Browser","text":"<ul> <li>Playwright (Chromium). Requires <code>playwright install chromium</code> (server) or <code>agentgate setup browser</code> (CLI).</li> <li>Element IDs are short and snapshot-scoped; the selector map never leaves the server.</li> <li>Submitting a form is <code>NEED_APPROVAL</code> by policy, including submit-capable clicks.</li> <li>Tuning: <code>PLAYWRIGHT_*</code>, <code>BROWSER_*</code> in configuration.</li> </ul>"},{"location":"connectors.html#adding-a-connector","title":"Adding a connector","text":"<ol> <li>Create <code>app/domains/connector/&lt;name&gt;/</code> with a class extending <code>BaseConnector</code>; return <code>ExecutionResult</code> and    <code>ConnectorError</code> on failure. Never log tokens.</li> <li>Register it in <code>APIExecutor.connectors</code> (<code>app/executors/api_executor.py</code>).</li> <li>Add each operation to the host tool table <code>_TOOLS</code> in <code>app/domains/guardrail/decision/agentgate.py</code> with its    trusted risk hints and the content fields that may be redacted. Unregistered operations are blocked.</li> <li>Add the system to <code>ALLOWED_TARGET_SYSTEMS</code> and <code>DOMAIN_BY_TARGET_SYSTEM</code> in settings.</li> <li>Add planner guidance for the new operations, config variables to <code>.env.example</code>, unit tests, and a section here.</li> </ol>"},{"location":"contracts.html","title":"Runtime contracts (schema v0.1)","text":"<p>Source: <code>app/core/action_schema.py</code>, <code>executor_schema.py</code>, <code>audit_schema.py</code>, <code>browser_schema.py</code>, <code>run_schema.py</code>, <code>errors.py</code>. All public models carry <code>schema_version = \"0.1\"</code>. IDs are <code>&lt;prefix&gt;_&lt;12 hex&gt;</code> (<code>run_</code>, <code>act_</code>, <code>aud_</code>, <code>snap_</code>, <code>guard_</code>).</p>"},{"location":"contracts.html#enums","title":"Enums","text":"Enum Values <code>Decision</code> <code>ALLOW</code>, <code>BLOCK</code>, <code>NEED_APPROVAL</code>, <code>SANITIZE</code>, <code>ASK_USER</code> <code>ExecutionStatus</code> <code>SUCCESS</code>, <code>FAILED</code>, <code>SKIPPED</code>, <code>BLOCKED</code>, <code>PENDING_APPROVAL</code>, <code>SANITIZED</code>, <code>WAITING_USER</code> <code>RiskLevel</code> <code>LOW</code>, <code>MEDIUM</code>, <code>HIGH</code>, <code>CRITICAL</code> <code>RunStatus</code> (interactive run) <code>running</code>, <code>waiting_approval</code>, <code>waiting_input</code>, <code>done</code>, <code>failed</code>, <code>blocked</code>, <code>declined</code>, <code>error</code>, <code>cancelled</code> <code>StepStatus</code> (interactive step) <code>pending</code>, <code>running</code>, <code>waiting_approval</code>, <code>waiting_input</code>, <code>approved</code>, <code>declined</code>, <code>done</code>, <code>failed</code>, <code>blocked</code>, <code>skipped</code> <code>ConnectorErrorCode</code> <code>AUTH</code>, <code>PERMISSION</code>, <code>RATE_LIMIT</code>, <code>TIMEOUT</code>, <code>VALIDATION</code>, <code>UNAVAILABLE</code>, <code>NOT_FOUND</code>, <code>UNKNOWN</code> <p><code>ExecutionStatus</code> describes the write-once audit outcome. <code>RunStatus</code>/<code>StepStatus</code> describe the live, interactive lifecycle above it.</p>"},{"location":"contracts.html#actionrequest","title":"ActionRequest","text":"<p>What the planner proposes.</p> Field Type Notes <code>run_id</code>, <code>action_id</code> str Generated if absent <code>source</code> str Default <code>cli</code> <code>domain</code> str <code>browser</code>, <code>productivity</code>, <code>code_protection</code>, <code>booking</code>, <code>filesystem</code>; defaults from <code>DOMAIN_BY_TARGET_SYSTEM</code> <code>action_type</code> str <code>API_CALL</code>, <code>FILE_READ</code>, <code>BROWSER_OPEN</code>, <code>BROWSER_CLICK</code>, <code>BROWSER_TYPE</code>, ... <code>target_system</code> str <code>local_file</code>, <code>github</code>, <code>gmail</code>, <code>calendar</code>, <code>telegram</code>, <code>stripe</code>, <code>browser</code> <code>target</code> str or object Summary of the target <code>recipient_reference</code>, <code>resolved_recipient</code> str? / object? Telegram recipient resolution, kept for audit <code>user_goal</code> str The originating instruction <code>content_context</code>, <code>payload_summary</code> str Short, redacted <code>payload</code> object Excluded from serialization, so it never reaches the audit record <code>browser_element</code> <code>BrowserElement</code>? <code>snapshot_id</code>, <code>element_id</code>, <code>role</code>, <code>label</code>, <code>text</code>, <code>risk_hint</code> <code>risk_hint</code> str Default <code>unknown</code>; see guardrail-policy <code>rollback_available</code> bool Default false <code>confidence</code> float 0-1 Default 1.0 <code>created_at</code> datetime (UTC)"},{"location":"contracts.html#decisionresponse","title":"DecisionResponse","text":"Field Type Notes <code>decision</code> <code>Decision</code> Final verdict <code>risk_level</code>, <code>risk_score</code> enum, float 0-1 <code>reasons</code> list[str] Human-readable rationale <code>triggered_policies</code> list[str] Policy rule IDs, e.g. <code>global.browser_submit</code> <code>sensitive_entities</code> list[str] Entity kinds only, never raw values <code>sanitized_payload</code> object? Redacted replacement, for <code>SANITIZE</code> (and approvals of redacted actions) <code>initial_decision</code> <code>Decision</code>? Verdict before an approval/input step changed it <code>approval_decision</code> str? Human outcome, when one occurred <code>guardrail_audit_id</code> str? Links to the <code>guardrail.jsonl</code> evaluation journal entry (<code>guard_...</code>) <code>evaluation_error</code> str? Set when a detector failed; forces approval <code>next_step</code> str <code>execute</code>, <code>blocked</code>, <code>sanitize</code>, <code>ask_user</code>, <code>approval_queue</code> <code>latency_ms</code> int"},{"location":"contracts.html#executionresult","title":"ExecutionResult","text":"<p><code>run_id</code>, <code>action_id</code>, <code>executor</code>, <code>status</code> (<code>ExecutionStatus</code>), <code>result_summary</code>, <code>data</code> (object), <code>error</code> (a serialized <code>ConnectorError</code> or null), <code>latency_ms</code>, <code>created_at</code>.</p>"},{"location":"contracts.html#connectorerror","title":"ConnectorError","text":"<p><code>code</code> (<code>ConnectorErrorCode</code>), <code>message</code>, <code>retryable</code> (bool), <code>details</code> (object).</p>"},{"location":"contracts.html#auditevent","title":"AuditEvent","text":"<p>One record per action (see audit-design).</p> Field Notes <code>audit_id</code> <code>aud_...</code> <code>run_id</code>, <code>action_id</code> <code>request_json</code>, <code>decision_json</code>, <code>execution_json</code> Serialized models above <code>execution_status</code> Copied from the execution result <code>error_type</code> The error <code>code</code>, if any <code>policy_version</code>, <code>detector_version</code> Default <code>policy-0.1</code>, <code>detector-0.1</code> <code>latency</code> <code>guardrail_ms</code>, <code>executor_ms</code>, <code>audit_write_ms</code>, <code>total_ms</code>, ... <code>created_at</code>"},{"location":"contracts.html#actiontrace","title":"ActionTrace","text":"<p>Model-ready export (JSONL): <code>run_id</code>, <code>action_id</code>, <code>user_goal</code>, <code>raw_tool_call</code>, <code>action_request</code>, <code>decision</code>, <code>execution</code>, <code>audit</code>, <code>latency</code>, <code>final_status</code>, <code>created_at</code>.</p>"},{"location":"contributing.html","title":"Contributing","text":""},{"location":"contributing.html#setup","title":"Setup","text":"<pre><code>git clone &lt;repo&gt; &amp;&amp; cd agent-gate\npython3.11 -m venv .venv &amp;&amp; source .venv/bin/activate\npip install -e \".[dev,server,browser,stripe]\"\ncp .env.example .env\npytest\n</code></pre>"},{"location":"contributing.html#workflow","title":"Workflow","text":"<ul> <li>Branch off <code>main</code>; never commit to <code>main</code>. Name branches <code>&lt;type&gt;/&lt;short-description&gt;</code>   (<code>feat/approval-queue</code>, <code>fix/audit-timestamp</code>).</li> <li>Use Conventional Commits: <code>feat:</code>, <code>fix:</code>, <code>docs:</code>, <code>refactor:</code>,   <code>test:</code>, <code>chore:</code>. Keep commits small and focused.</li> <li>One concern per pull request. Describe the change, how you tested it, and any migration or config impact.</li> <li>Record significant architectural decisions as an ADR in <code>docs/decisions/</code>.</li> <li>At least one review approval; squash-merge into <code>main</code>.</li> </ul>"},{"location":"contributing.html#before-you-push","title":"Before you push","text":"<pre><code>ruff check .\nblack --check .          # `black .` to fix\nmypy app\npytest\n</code></pre> <p>Line length is 100 (ruff and black). The vendored guardrail (<code>app/domains/guardrail/_vendor</code>) is excluded from lint and formatting; do not reformat it. It is a pinned copy of an upstream project; see its <code>NOTICE.md</code>.</p>"},{"location":"contributing.html#where-code-goes","title":"Where code goes","text":"Change Location New HTTP endpoint <code>app/api/v1/</code>, with logic in a domain service (never in the router) Business logic <code>app/domains/&lt;domain&gt;/</code> Shared contracts <code>app/core/</code> (bump <code>schema_version</code> for breaking changes) New connector <code>app/domains/connector/&lt;name&gt;/</code>; follow connectors.md Guardrail rules Policy pack JSON, or <code>decision/</code> for host rules Config <code>app/config/settings.py</code> plus <code>.env.example</code> and configuration.md Migrations <code>alembic revision --autogenerate -m \"...\"</code>; import new models in <code>app/database/models/__init__.py</code>"},{"location":"contributing.html#rules-of-the-road","title":"Rules of the road","text":"<ul> <li>Every proposed action becomes an <code>ActionRequest</code> before execution, and every executed or skipped action is audited.</li> <li>Never log secrets, tokens, or private payloads. <code>ActionRequest.payload</code> must stay out of serialized records.</li> <li>Keep domains decoupled; cross-domain calls go through services or the shared contracts.</li> <li>Prefer failing closed: unknown operations are blocked, detector errors require approval.</li> <li>Never commit secrets. Only <code>.env.example</code> is meant to be tracked (<code>.env.development</code> and <code>.env.staging</code> also   are, so keep them free of real values).</li> </ul>"},{"location":"contributing.html#documentation","title":"Documentation","text":"<p>Update the relevant page in <code>docs/</code> in the same pull request: API changes in <code>api-reference.md</code>, settings in <code>configuration.md</code>, schema changes in <code>contracts.md</code> and <code>data-model.md</code>, connector changes in <code>connectors.md</code>.</p>"},{"location":"data-model.html","title":"Data model","text":""},{"location":"data-model.html#server-postgresql-tables","title":"Server (PostgreSQL) tables","text":"<p>Created by Alembic migrations <code>0001</code>-<code>0004</code>.</p>"},{"location":"data-model.html#audit_logs-migration-0001","title":"<code>audit_logs</code> (migration 0001)","text":"<p>One immutable row per action. See audit-design.</p> Column Type Notes <code>audit_id</code> varchar(32) PK <code>run_id</code> varchar(32), indexed <code>action_id</code> varchar(32), unique <code>request_json</code>, <code>decision_json</code>, <code>execution_json</code> JSONB Request excludes <code>payload</code> <code>execution_status</code> varchar(32) <code>error_type</code> varchar(64), null <code>policy_version</code>, <code>detector_version</code> varchar(32) Default <code>policy-0.1</code>, <code>detector-0.1</code> <code>latency</code> JSONB Default <code>{}</code> <code>created_at</code> timestamptz Default <code>now()</code> <p>Trigger <code>trg_audit_logs_immutable</code> raises an exception on any UPDATE or DELETE.</p>"},{"location":"data-model.html#oauth_tokens-0002","title":"<code>oauth_tokens</code> (0002)","text":"Column Type Notes <code>provider</code> varchar(32) PK <code>github</code>, <code>gmail</code>, <code>calendar</code> <code>access_token</code> text Stored as plain text <code>refresh_token</code> text, null Stored as plain text <code>expires_at</code> timestamptz, null <code>scope</code> text, null <code>updated_at</code> timestamptz"},{"location":"data-model.html#telegram_contacts-0003","title":"<code>telegram_contacts</code> (0003)","text":"<p><code>id</code> PK; <code>chat_id</code> (bigint, unique); <code>chat_type</code>; <code>username</code>, <code>first_name</code>, <code>last_name</code>, <code>display_name</code> (indexed where noted); <code>is_active</code>; <code>first_seen_at</code>; <code>last_seen_at</code>. Filled from inbound messages and used to resolve human recipient names to numeric chat IDs.</p>"},{"location":"data-model.html#stripe_payments-and-stripe_webhook_events-0004","title":"<code>stripe_payments</code> and <code>stripe_webhook_events</code> (0004)","text":"Table Columns <code>stripe_payments</code> <code>id</code>, <code>stripe_session_id</code>, <code>payment_intent_id</code>, <code>run_id</code>, <code>action_id</code>, <code>status</code>, <code>amount</code> (bigint), <code>currency</code>, <code>created_at</code>, <code>updated_at</code> <code>stripe_webhook_events</code> <code>id</code>, <code>stripe_event_id</code> (unique, gives idempotency), <code>event_type</code> (indexed), <code>livemode</code>, <code>processed_at</code>"},{"location":"data-model.html#local-cli-stores","title":"Local CLI stores","text":"Store Location Content <code>state.sqlite3</code> <code>~/.local/share/agentgate/</code> (or <code>AGENTGATE_DATA_DIR</code>) Run history, safe audit/traces, OAuth metadata, payment status, execution intents. Append-only audit via SQLite triggers <code>guardrail.jsonl</code> Same directory One record per guardrail evaluation (the evaluation journal, ADR 0003) <code>config.toml</code> <code>~/.config/agentgate/</code> (or <code>AGENTGATE_CONFIG_DIR</code>) Non-secret settings OS keychain Tokens and keys (or environment / session, by credential mode)"},{"location":"data-model.html#execution-intents-cli","title":"Execution intents (CLI)","text":"<p>Per ADR 0002, an intent journal records an approved operation before its external effect and its outcome afterward. If the intent cannot be durably recorded, the write is not performed. Financial operations also persist a stable operation identity and idempotency data first. The journal holds no customer email, passwords, tokens, or raw browser input. An ambiguous remote outcome stays <code>unknown</code> until reconciled (<code>agentgate payments sync</code>). Crash recovery marks abandoned runs interrupted and never replays payments, messages, or calendar writes.</p>"},{"location":"data-model.html#jsonl-files-server-optional","title":"JSONL files (server, optional)","text":"File Written when <code>artifacts/audit/events.jsonl</code> <code>AUDIT_BACKEND=jsonl</code> <code>artifacts/traces/actions.jsonl</code> Every guarded action <code>artifacts/audit/guardrail.jsonl</code> Every guardrail evaluation (server default path). Server deployments must persist this file (ADR 0003) <p><code>/artifacts/audit/</code> and <code>/artifacts/traces/</code> are gitignored.</p>"},{"location":"data-model.html#what-is-never-stored","title":"What is never stored","text":"<p><code>ActionRequest.payload</code> is excluded from serialized audit records. Sensitive entities are recorded as kinds, not values. Guardrail journal entries pass through a sanitizer. See security for the exceptions (OAuth tokens in the database).</p>"},{"location":"deployment.html","title":"Deployment and operations","text":""},{"location":"deployment.html#topologies","title":"Topologies","text":"Topology Components Notes Local CLI <code>agentgate</code> + Ollama Single user, no server Single host API (uvicorn) + PostgreSQL + Ollama Simplest server deployment Docker Compose <code>api</code>, <code>postgres</code>; Ollama on host Provided <code>docker-compose.yml</code> <p>The design is a single deployable unit. Horizontal scaling (Redis queue and workers) is planned in ADR 0001 but not implemented. Because run state and OAuth <code>state</code> are in process memory, run one API worker.</p>"},{"location":"deployment.html#container","title":"Container","text":"<ul> <li><code>Dockerfile</code> is multi-stage: it builds a venv with <code>.[server,browser,stripe]</code>, installs Chromium and its   system libraries, runs as non-root <code>appuser</code> (uid 1000), and starts <code>uvicorn app.main:app</code> on port 8000.</li> <li><code>docker-compose.yml</code>: <code>postgres</code> (<code>pgvector/pgvector:pg17</code>, volume <code>postgres_data</code>, healthcheck) and <code>api</code>   (<code>.env</code>, <code>host.docker.internal</code> mapped to the host gateway for Ollama). The repo is bind-mounted at <code>/app</code> in   the provided Compose file, which suits development; remove the mount for production images.</li> <li>The Compose file reads <code>DB_PASSWORD</code> (default <code>agentgate</code>). Set your own.</li> <li>Persist the guardrail journal. ADR 0003 requires server deployments to keep the evaluation journal   (<code>GUARDRAIL_AUDIT_PATH</code>, default <code>artifacts/audit/guardrail.jsonl</code>). The provided Compose file only preserves it   because the repo is bind-mounted. In a production image, mount a volume for <code>artifacts/</code> (and <code>data/</code> for   screenshots), otherwise the journal is lost with the container.</li> </ul> <pre><code>docker compose up --build -d\ndocker compose exec api alembic upgrade head\ndocker compose logs -f api\n</code></pre>"},{"location":"deployment.html#release-checklist","title":"Release checklist","text":"<ol> <li><code>pytest</code> passes (see testing).</li> <li><code>ruff check .</code>, <code>black --check .</code>, <code>mypy app</code> pass.</li> <li>Migrations reviewed; <code>alembic upgrade head</code> tested on a copy of production data.</li> <li><code>.env</code> for the target environment: <code>APP_ENV</code>, <code>DATABASE_URL</code>, <code>LLM_*</code>, <code>AUDIT_BACKEND=postgres</code>,    webhook secrets, <code>OLLAMA_HOST</code>, connector credentials.</li> <li>Ollama reachable with the detector model pulled.</li> <li>Health check <code>GET /api/v1/health</code> returns ok.    The evaluation journal path is writable and on a persistent volume (a failed journal write blocks execution).</li> <li>One guarded run of each critical path (file read, an approval, a blocked action).</li> </ol>"},{"location":"deployment.html#migrations","title":"Migrations","text":"<pre><code>alembic upgrade head                       # apply\nalembic current                            # show revision\nalembic revision --autogenerate -m \"msg\"   # after model changes (models are imported in migrations/env.py)\nalembic downgrade -1                       # roll back one\n</code></pre> <p><code>downgrade</code> of <code>0001</code> drops <code>audit_logs</code> and its trigger, which destroys the audit history. Never run it on production without a backup. For remote databases set <code>DATABASE_SSL_MODE</code>; <code>scripts/check_database_connection.py</code> tests connectivity without printing credentials.</p>"},{"location":"deployment.html#monitoring","title":"Monitoring","text":"Signal How Liveness <code>GET /api/v1/health</code> Logs Structured JSON to stdout (<code>python-json-logger</code>); set <code>LOG_LEVEL</code> Throughput / latency <code>GET /api/v1/benchmark</code>; <code>latency</code> fields on audit records; JSONL traces Decisions Count of <code>BLOCKED</code>, <code>PENDING_APPROVAL</code>, <code>FAILED</code> in <code>audit_logs.execution_status</code> Guardrail health <code>guardrail.jsonl</code>; <code>evaluation_error</code> in decisions; <code>agentgate doctor</code> for Ollama Pending approvals <code>GET /api/v1/approvals</code> <p>Useful queries:</p> <pre><code>SELECT execution_status, count(*) FROM audit_logs GROUP BY 1 ORDER BY 2 DESC;\nSELECT run_id, action_id, error_type, created_at FROM audit_logs\n WHERE execution_status = 'FAILED' ORDER BY created_at DESC LIMIT 50;\n</code></pre>"},{"location":"deployment.html#backup-and-restore","title":"Backup and restore","text":"<ul> <li>PostgreSQL: <code>pg_dump -Fc agentgate &gt; agentgate.dump</code>; restore with <code>pg_restore</code>. The immutability   trigger is part of the schema; verify it after a restore.</li> <li>JSONL: copy <code>artifacts/audit/</code> (includes <code>guardrail.jsonl</code>) and <code>artifacts/traces/</code> (both gitignored).</li> <li>CLI: back up <code>AGENTGATE_DATA_DIR</code> (<code>state.sqlite3</code>, <code>guardrail.jsonl</code>) and <code>config.toml</code>.</li> <li>Include <code>oauth_tokens</code> in the same protection level as the credentials it contains.</li> </ul>"},{"location":"deployment.html#runbook","title":"Runbook","text":"Symptom Check Action Every run pauses for approval <code>agentgate doctor</code>; is Ollama up and the model pulled? Start Ollama / <code>ollama pull qwen2.5:7b</code>; check <code>OLLAMA_HOST</code> (Docker: <code>host.docker.internal</code>) Detector timeouts <code>AGENTGATE_LLM_DETECTOR_TIMEOUT</code>, <code>OLLAMA_NUM_PARALLEL</code> Raise timeout, set parallelism to 6, use a lighter model 5xx on <code>/audits</code>, <code>/runs</code> DB reachable? <code>AUDIT_BACKEND</code> Fix <code>DATABASE_URL</code>, or switch to <code>jsonl</code> temporarily Migration fails on TLS <code>DATABASE_SSL_MODE</code> <code>require</code> for remote providers OAuth callback error Single worker? redirect URI exact match? Restart flow within seconds; align redirect URI with the provider console Telegram webhook 403/503 <code>TELEGRAM_WEBHOOK_SECRET</code> set and equal to the registered secret Re-run <code>scripts/set_telegram_webhook.py</code> Stripe webhook 400 <code>STRIPE_WEBHOOK_SECRET</code>, clock skew Fix secret; tolerance is <code>STRIPE_WEBHOOK_TOLERANCE_SEC</code> Browser actions fail in container Chromium installed? memory? Rebuild image; reduce <code>BROWSER_MAX_CONCURRENT_SESSIONS</code> Approval never arrives <code>AGENT_WAIT_RESPONSE_TIMEOUT_SEC</code> (600) Respond in time or raise the timeout Restart lost live runs Expected: run state is in memory Re-run; audit rows remain Disk fills <code>artifacts/</code> and screenshots grow (no cleanup job) Rotate manually"},{"location":"deployment.html#rollback","title":"Rollback","text":"<ul> <li>Code: redeploy the previous image or commit.</li> <li>Guardrail: <code>GUARDRAIL_BACKEND=legacy</code> (deliberate, documented rollback; weaker detection).</li> <li>Audit backend: <code>AUDIT_BACKEND=jsonl</code> if PostgreSQL is unavailable (history splits across stores).</li> <li>Schema: prefer forward fixes; see the downgrade warning above.</li> </ul>"},{"location":"getting-started.html","title":"Getting started","text":"<p>Pick a path. A (CLI) is the fastest. B (server) gives you the HTTP API. C runs the server in Docker.</p>"},{"location":"getting-started.html#0-prerequisites","title":"0. Prerequisites","text":"Requirement Needed for Check Python 3.11+ everything <code>python --version</code> An LLM API key the planner (OpenAI-compatible, Anthropic, or Gemini dialect) see <code>LLM_*</code> in configuration Ollama the default guardrail's detectors <code>ollama --version</code> Docker + Compose server with bundled PostgreSQL (optional) <code>docker compose version</code> PostgreSQL 17 server with <code>AUDIT_BACKEND=postgres</code> via Docker or your own"},{"location":"getting-started.html#prepare-the-guardrail-model-all-paths","title":"Prepare the guardrail model (all paths)","text":"<p>The default guardrail (<code>GUARDRAIL_BACKEND=agentgate</code>) runs six detectors on a local Ollama model.</p> <pre><code>ollama pull qwen2.5:7b\nOLLAMA_NUM_PARALLEL=6 ollama serve        # PowerShell: $env:OLLAMA_NUM_PARALLEL = \"6\"\n</code></pre> <p>If Ollama is unreachable, guarded runs pause for approval instead of silently allowing actions. <code>GUARDRAIL_BACKEND=legacy</code> is an explicit rollback to the older rule engine.</p>"},{"location":"getting-started.html#what-pip-installs","title":"What pip installs","text":"<p><code>pip install .</code> installs only the core packages. Everything else is an optional extra:</p> Extra Adds Needed for (core) sqlalchemy, pydantic, pydantic-settings, httpx, PyYAML, platformdirs, filelock, keyring, python-json-logger CLI and shared code <code>server</code> fastapi, uvicorn, alembic, psycopg, asyncpg, redis HTTP API, migrations <code>browser</code> playwright Browser actions <code>stripe</code> stripe[async] Stripe connector <code>dev</code> pytest, pytest-asyncio, ruff, black, mypy, aiosqlite Tests and linting <p>Not installed by pip: Chromium (<code>playwright install chromium</code>), Ollama, PostgreSQL, Docker.</p>"},{"location":"getting-started.html#path-a-local-cli","title":"Path A: Local CLI","text":"<p>No server, Docker, PostgreSQL, or Redis.</p> <pre><code>pipx install .                 # from the repo root\nagentgate init                 # LLM dialect, endpoint/model, timezone, workspace, credential store\nagentgate doctor               # credentials, workspace, storage, browser, Ollama readiness\nagentgate run \"Read README.md\"\nagentgate history\nagentgate show &lt;run-id&gt;\n</code></pre> <p>Headless setup:</p> <pre><code>agentgate init --non-interactive --workspace /absolute/path/to/project --credential-store env\nexport LLM_API_KEY=...         # supply secrets through the environment, never CLI flags\n</code></pre> <p>Optional:</p> <pre><code>agentgate connect github | gmail | calendar | stripe | llm\nagentgate setup browser        # downloads Chromium, enables browser actions\nagentgate payments sync        # refresh tracked Stripe checkout/refund status\n</code></pre> <p>The CLI does not read a project <code>.env</code>. Full guide: cli.md.</p>"},{"location":"getting-started.html#path-b-server-fastapi","title":"Path B: Server (FastAPI)","text":""},{"location":"getting-started.html#1-install","title":"1. Install","text":"<pre><code>python3.11 -m venv .venv &amp;&amp; source .venv/bin/activate      # Windows: .venv\\Scripts\\activate\npip install -e \".[dev,server,browser,stripe]\"\npython -m playwright install chromium                       # if you use browser actions\n</code></pre> <p>Or with uv: <code>uv sync --extra dev --extra server --extra browser --extra stripe</code>.</p>"},{"location":"getting-started.html#2-configure","title":"2. Configure","text":"<pre><code>cp .env.example .env\n</code></pre> <p>Minimum edits:</p> Variable Set it to <code>LLM_URL</code>, <code>LLM_MODEL</code>, <code>LLM_API_KEY</code> Your provider's endpoint, model, key <code>DATABASE_URL</code> Your PostgreSQL URL <code>AUDIT_BACKEND</code> <code>postgres</code>, or <code>jsonl</code> to run with no database <p><code>.env.example</code> uses <code>postgresql+asyncpg://</code>; the code default and <code>docker-compose.yml</code> use <code>postgresql+psycopg://</code>. Both drivers are installed with the <code>server</code> extra and both work. Keep <code>.env</code> and Compose consistent.</p> <p>Quickest setup with no database: <code>AUDIT_BACKEND=jsonl</code>. Audit events go to <code>artifacts/audit/events.jsonl</code>, traces to <code>artifacts/traces/actions.jsonl</code>.</p>"},{"location":"getting-started.html#3-start-postgresql-skip-if-audit_backendjsonl","title":"3. Start PostgreSQL (skip if <code>AUDIT_BACKEND=jsonl</code>)","text":"<pre><code>docker compose up -d postgres\n</code></pre>"},{"location":"getting-started.html#4-migrate","title":"4. Migrate","text":"<pre><code>alembic upgrade head\n</code></pre> <p>Creates <code>audit_logs</code> (immutable via trigger), <code>oauth_tokens</code>, <code>telegram_contacts</code>, <code>stripe_payments</code>, <code>stripe_webhook_events</code>. See data-model.md.</p>"},{"location":"getting-started.html#5-start-the-api","title":"5. Start the API","text":"<pre><code>uvicorn app.main:app --reload\npython run.py                   # Windows: sets the event-loop policy Playwright needs\n</code></pre>"},{"location":"getting-started.html#6-verify","title":"6. Verify","text":"<pre><code>curl http://localhost:8000/api/v1/health       # {\"status\":\"ok\"}\ncurl http://localhost:8000/                    # service, env, version\n</code></pre> <p>Swagger UI: http://localhost:8000/docs.</p>"},{"location":"getting-started.html#7-try-a-guarded-run","title":"7. Try a guarded run","text":"<pre><code>curl -X POST http://localhost:8000/api/v1/chat/execute \\\n  -H \"Content-Type: application/json\" -d '{\"prompt\":\"Read file sample.txt\"}'\n# -&gt; {\"run_id\":\"run_...\",\"status\":\"running\",\"prompt\":\"...\"}\n\ncurl http://localhost:8000/api/v1/chat/execute/&lt;run_id&gt;\ncurl \"http://localhost:8000/api/v1/audits?run_id=&lt;run_id&gt;\"\n</code></pre> <p>If a step needs approval:</p> <pre><code>curl -X POST http://localhost:8000/api/v1/chat/execute/&lt;run_id&gt;/respond \\\n  -H \"Content-Type: application/json\" -d '{\"step_index\":0,\"action\":\"approve\"}'\n</code></pre> <p>A static browser demo lives in <code>fe/agent-gate-demo.html</code> (CORS is open for it).</p>"},{"location":"getting-started.html#path-c-docker-compose-api-postgresql","title":"Path C: Docker Compose (API + PostgreSQL)","text":"<pre><code>cp .env.development .env         # or .env.example\ndocker compose up --build\ndocker compose exec api alembic upgrade head\n</code></pre> <p>The image installs <code>.[server,browser,stripe]</code> and Chromium. Compose defines <code>postgres</code> and <code>api</code>; the API listens on port 8000. The container reaches Ollama on the host at <code>http://host.docker.internal:11434</code>, so start Ollama on the host first.</p>"},{"location":"getting-started.html#scripts","title":"Scripts","text":"<pre><code>python scripts/run_demo_scenario.py local_file_read\npython scripts/run_demo_scenario.py browser_snapshot\npython scripts/export_audit.py --latest\npython scripts/export_traces.py --latest\npython scripts/check_database_connection.py        # tests DATABASE_URL without printing secrets\n</code></pre>"},{"location":"getting-started.html#troubleshooting","title":"Troubleshooting","text":"Symptom Likely cause / fix Runs keep pausing for approval Ollama not running or model not pulled; run <code>agentgate doctor</code> <code>ModuleNotFoundError: fastapi</code> / <code>alembic</code> Installed without extras; use <code>pip install -e \".[server]\"</code> <code>connection refused</code> on the DB PostgreSQL not up, or wrong host/driver in <code>DATABASE_URL</code> Playwright <code>NotImplementedError</code> (Windows) Start with <code>python run.py</code> Browser actions fail <code>playwright install chromium</code> / <code>agentgate setup browser</code> File read refused Path outside <code>LOCAL_FILE_ROOT</code> and <code>ALLOWED_FILESYSTEM_PATHS</code> Run state gone after restart Live run state is in memory; only audit rows persist Remote DB TLS errors Set <code>DATABASE_SSL_MODE=require</code> (or <code>auto</code> for Neon)"},{"location":"glossary.html","title":"Glossary","text":"Term Meaning Action One operation the planner proposes (read a file, send a message, click a button) ActionRequest The validated description of an action, before evaluation Agent loop Plan, guardrail, execute, observe, replan cycle for a run Approval A human decision to run (or decline) an action the guardrail flagged as <code>NEED_APPROVAL</code> Audit event / row The immutable record of an action's request, decision, and outcome BLOCK Verdict: never execute Connector Adapter that performs an operation on an external system (<code>BaseConnector</code>) Decision router <code>ExecutionRouter</code>: sends allowed actions to the API or browser executor Detector An LLM-backed check (PII, secrets, source code, payment phishing, prompt injection, action intent) Domain Risk category of an action: <code>browser</code>, <code>productivity</code>, <code>code_protection</code>, <code>booking</code>, <code>filesystem</code> Executor Component that runs an action: <code>APIExecutor</code> or <code>BrowserExecutor</code> Fail closed On error or uncertainty, require approval or block instead of allowing Guardrail The evaluation layer that produces a <code>DecisionResponse</code> Guardrail journal <code>guardrail.jsonl</code>: one entry per guardrail evaluation Host rules Application-enforced rules (registered operations, trusted hints, secret egress) that detectors cannot override Policy pack JSON file of rules mapping conditions to a decision and a risk floor Redaction / sanitize Masking sensitive content so an action can proceed with a safe payload Rollback available Whether an action can be undone; irreversible actions raise risk Risk hint A label (<code>external_send</code>, <code>payment</code>, <code>destructive</code>, ...) describing an action's impact Run One user request handled by the agent loop, identified by <code>run_id</code> Sanitize pause A run waiting for the user to type a missing or sensitive value (<code>waiting_input</code>) Selector map Server-side map from short element IDs to real browser locators; never shown to the planner Step One planned action within a run Trace <code>ActionTrace</code>: model-ready export of the whole action chain"},{"location":"guardrail-policy.html","title":"Guardrail policy","text":"<p>How AgentGate turns a proposed action into a verdict. Source: <code>app/domains/guardrail/</code>. Engine details: integrations/guardrail.md. Why it is embedded: ADR 0003.</p> <p>The engine is a private vendored copy of the upstream <code>NafisNaufal/agentgate</code> core, pinned at revision <code>9b919c604d2a0955fff26b4f05045281514c0220</code>. The application adapts its own action contract and trusted connector metadata to it and maps the result back with existing policy floors. Every evaluation is recorded in a durable guardrail journal before it reaches execution; if that record cannot be written, the action does not run.</p>"},{"location":"guardrail-policy.html#backends","title":"Backends","text":"<code>GUARDRAIL_BACKEND</code> Behaviour <code>agentgate</code> (default) Embedded upstream engine: six Ollama detectors + policy packs + scoring, combined with the application rule engine <code>legacy</code> Deterministic rules only, plus an optional LLM second opinion (<code>GUARDRAIL_LLM_ENABLED</code>). Must be selected explicitly; there is no automatic fallback"},{"location":"guardrail-policy.html#verdict-resolution-agentgate-backend","title":"Verdict resolution (<code>agentgate</code> backend)","text":"<p>Two verdicts are computed and merged: the upstream engine (detectors + policy packs) and the application rules (a minimum floor from trusted connector metadata). The merge order is:</p> <ol> <li><code>BLOCK</code> wins over everything.</li> <li><code>ASK_USER</code> next: approving an action whose intent is unknown is not meaningful.</li> <li>A detector failure (<code>evaluation_error</code>) forces <code>NEED_APPROVAL</code>.</li> <li><code>NEED_APPROVAL</code> before <code>SANITIZE</code>: approval must occur before redacted content can run.</li> <li><code>SANITIZE</code>, then <code>ALLOW</code>.</li> </ol> <p>Risk score and level take the maximum of the two sides.</p>"},{"location":"guardrail-policy.html#host-enforced-rules","title":"Host-enforced rules","text":"<p>These hold regardless of what the planner claims or the detectors return:</p> Rule Effect Registered operations only Only known <code>(target_system, operation)</code> pairs run (<code>local_file.read</code>, <code>github.repo_metadata</code>, <code>gmail.list_messages</code>, <code>calendar.list_events/create_event</code>, <code>telegram.*</code>, <code>stripe.*</code>). Anything else is <code>BLOCK</code> with policy <code>host.registered_tool_required</code> Trusted risk hints per operation e.g. calendar create and Telegram sends carry <code>external_send</code>; Stripe checkout <code>payment</code>; refunds <code>refund</code>; expiring a session <code>payment</code> + <code>destructive_action</code>. The planner's own <code>risk_hint</code> can only raise the floor, not lower it Secrets never leave If a credential entity appears in an <code>API_CALL</code>, <code>BROWSER_SUBMIT</code>, or <code>BROWSER_TYPE</code> action: <code>BLOCK</code> (<code>code.secret_egress</code>) Unredactable secrets A secret that cannot be rewritten into a supported content field is <code>BLOCK</code> (<code>host.unredactable_sensitive_content</code>) SANITIZE without a replacement Downgraded to <code>NEED_APPROVAL</code> Detector failure Unavailable or malformed detector response means approval is required; failing to persist the evaluation prevents execution Payment in browser flows Payment-related browser actions add the <code>payment</code> hint <p>Detector prompts receive a copy with known credentials already masked, so secrets are not sent to Ollama.</p>"},{"location":"guardrail-policy.html#application-rule-priority-deterministic-rules","title":"Application rule priority (deterministic rules)","text":"<p>Applied in order; first match wins (<code>app/domains/guardrail/decision/simple.py</code>):</p> # Condition Decision Risk 1 <code>risk_hint</code> in <code>GUARDRAIL_BLOCK_HINTS</code> <code>BLOCK</code> CRITICAL, 0.95 2 Secret-shaped content in payload <code>SANITIZE</code> (redacted preview) MEDIUM, 0.50 3 <code>risk_hint</code> in <code>GUARDRAIL_ASK_USER_HINTS</code> <code>ASK_USER</code> LOW, 0.30 4 <code>risk_hint</code> in <code>GUARDRAIL_NEED_APPROVAL_HINTS</code>, or domain risk is CRITICAL/HIGH <code>NEED_APPROVAL</code> domain risk (CRITICAL for <code>payment</code>/<code>refund</code>), 0.60 / 0.80 5 Otherwise <code>ALLOW</code> LOW, 0.10 <p>Domain base risk: <code>booking</code> CRITICAL, <code>code_protection</code> HIGH, <code>productivity</code> MEDIUM, <code>browser</code> LOW, <code>filesystem</code> LOW. Because <code>booking</code> (Stripe) and <code>code_protection</code> (GitHub) are high risk, actions in those domains need approval unless a more specific rule applies.</p> <p>Secret patterns redacted: <code>sk-...</code> keys, Stripe <code>sk_/rk_</code> keys and <code>whsec_</code> secrets, AWS <code>AKIA...</code>, GitHub <code>gh?_...</code> tokens, and <code>password|secret|api_key|token = value</code> assignments.</p>"},{"location":"guardrail-policy.html#policy-packs","title":"Policy packs","text":"<p>JSON rule packs in <code>app/domains/guardrail/_vendor/agentgate/policy/packs/</code>:</p> Pack Rules (decision) <code>global_safety</code> Prompt injection (BLOCK); credential-request phishing (BLOCK); bulk PII egress (BLOCK); destructive action (NEED_APPROVAL, higher if no rollback); low planner confidence <code>&lt; 0.5</code> on <code>API_CALL</code>/<code>BROWSER_SUBMIT</code>/<code>BROWSER_CLICK</code> (NEED_APPROVAL); PII in external action (SANITIZE); browser submit and submit-via-click (NEED_APPROVAL) <code>code_data</code> Secret egress (BLOCK); secret present (SANITIZE); env/credentials file access (NEED_APPROVAL); source code + external send (NEED_APPROVAL); local file write (NEED_APPROVAL); local file delete (NEED_APPROVAL) <code>productivity</code> Bulk action (NEED_APPROVAL, higher if irreversible); external email send (NEED_APPROVAL); calendar create/update (ALLOW) <code>booking</code> External payment message (NEED_APPROVAL); browser submit in booking flow (NEED_APPROVAL); customer PII to external (SANITIZE); cancelling a booking (NEED_APPROVAL) <p>A rule can set a decision and a <code>risk_floor</code>. Rules match on domains, action types, target systems, risk hints, entity kinds, tags, <code>requires_no_rollback</code>, and <code>min_confidence</code>.</p>"},{"location":"guardrail-policy.html#detectors","title":"Detectors","text":"<p>Six detectors run on the local Ollama model, dispatched concurrently: PII, secrets, source code, payment phishing, prompt injection, action intent. <code>AGENTGATE_DETECTOR_ARCHITECTURE=unified</code> switches to one experimental combined detector.</p>"},{"location":"guardrail-policy.html#sensitive-input-sanitize-pause","title":"Sensitive input (\"sanitize\" pause)","text":"<p>Separate from redaction. <code>app/domains/guardrail/sensitive.py</code> detects when a plan step needs the user to type something and pauses the run (<code>waiting_input</code>) until <code>/respond</code> with <code>action=input</code>:</p> <ul> <li>a value that is a placeholder: <code>{{name}}</code>, <code>{name}</code>, <code>&lt;name&gt;</code>, <code>[name]</code></li> <li>a fabricated fill-in: <code>YOUR_PASSWORD_HERE</code>, or a bare <code>password</code>, <code>token</code>, <code>email</code>, <code>otp</code>, ...</li> <li>a secret-named key (<code>password</code>, <code>token</code>, <code>api_key</code>, <code>otp</code>, <code>pin</code>, <code>cvv</code>, <code>card_number</code>, ...) with an empty value</li> <li>an empty <code>value</code> on <code>BROWSER_TYPE</code>/<code>BROWSER_SELECT</code></li> </ul> <p>Keys <code>label</code>, <code>role</code>, <code>element_id</code> are descriptive and never treated as fill-ins. Fields already answered are not asked again.</p>"},{"location":"guardrail-policy.html#human-interaction","title":"Human interaction","text":"Situation What happens <code>NEED_APPROVAL</code> Run pauses; approve executes (with the redacted payload if one exists), decline stops the step <code>SANITIZE</code> User reviews/confirms the redacted preview or supplies missing input <code>ASK_USER</code> Clarification is requested; it is not approval of a calendar or financial write CLI, non-interactive Required approval or input fails closed with exit code 3; there is no <code>--yes</code> bypass"},{"location":"guardrail-policy.html#tuning","title":"Tuning","text":"<ul> <li>Change which hints map to which verdict with <code>GUARDRAIL_BLOCK_HINTS</code>, <code>GUARDRAIL_NEED_APPROVAL_HINTS</code>,   <code>GUARDRAIL_ASK_USER_HINTS</code> (values must come from <code>ALLOWED_RISK_HINTS</code>, plus the ASK_USER set).</li> <li>Add or edit policy pack rules in the pack JSON files. Bump <code>policy_version</code> in audit records when you do   (currently the constant <code>policy-0.1</code>).</li> <li>Prefer stricter over looser: the host rules mean loosening a hint cannot unblock an unregistered operation.</li> </ul>"},{"location":"limitations.html","title":"Known limitations and inconsistencies","text":"<p>Findings from reading the repository. Verified means confirmed in code. Unverified means not confirmed and worth checking before relying on it.</p>"},{"location":"limitations.html#audit-and-approvals","title":"Audit and approvals","text":"<ul> <li>Action-sourced audit only (verified). The event-sourced v4 spec is not implemented (audit-design,   ADR 0004).</li> <li>No approval timeline (verified). The approval outcome is stored on the final row; when approval was   requested and decided is not recorded as separate events.</li> <li><code>PENDING_APPROVAL</code> snapshots (verified). <code>POST /actions/run</code> writes a pending row that nothing resumes;   <code>action_id</code> is unique so it cannot be superseded.</li> <li>Timeouts leave no row (verified). If a paused step times out waiting for a response, the loop returns   without writing an audit row.</li> <li>Audit write failures can be swallowed (verified). For declined, blocked, and skipped steps the loop logs a   warning and continues if the audit write fails.</li> <li>Browser batches (verified). By default consecutive browser steps produce one combined row; enable   <code>ATOMIC_BROWSER_AUDIT</code> for per-step rows.</li> <li>Live run state is in memory (verified). It does not survive a restart.</li> </ul>"},{"location":"limitations.html#configuration-and-setup","title":"Configuration and setup","text":"<ul> <li>Database driver (verified): <code>.env.example</code> uses <code>asyncpg</code>, code default and Compose use <code>psycopg</code>. Both   are supported; keep them consistent.</li> <li><code>GUARDRAIL_LLM_ENABLED</code> default (verified): <code>True</code> in code (only affects the legacy backend) but <code>False</code> in   <code>.env.example</code>, and the code comment says \"defaults to OFF\".</li> <li>Retention settings do nothing (verified): <code>AUDIT/TRACE/SCREENSHOT_RETENTION_DAYS</code> are defined and set per   profile, but no code reads them. There is no cleanup job. ADR 0002 explicitly defers automated retention.</li> <li>Unused setting (verified): <code>XENDIT_API_KEY</code> is defined with no code using it.</li> <li>Redis (verified): <code>redis</code> is a dependency and ADR 0001 lists a queue layer, but the Compose file has no Redis   service and no Redis code path was found.</li> <li>Missing file (verified): the README links <code>AgentGate Technical Foundation Document.md</code>, which is not in   the repository. The old README also says \"Sprint 1\"; <code>sprint.md</code> describes a <code>backend/...</code> layout that differs   from the real <code>app/...</code> layout.</li> <li>Windows event loop (verified): use <code>python run.py</code> so Playwright can spawn a subprocess.</li> </ul>"},{"location":"limitations.html#security","title":"Security","text":"<ul> <li>No API authentication or authorization; CORS is <code>*</code>.</li> <li>OAuth tokens are stored unencrypted; OAuth <code>state</code> is in process memory (single worker only).</li> <li><code>.env.development</code> and <code>.env.staging</code> are tracked in git.</li> <li>Full list and mitigations: security.</li> </ul>"},{"location":"limitations.html#product-scope","title":"Product scope","text":"<ul> <li>Connector breadth (verified): Gmail is read-only (<code>list_messages</code>), GitHub is <code>repo_metadata</code> only, Calendar   has list/create, Local files are read-only. There is no email send, GitHub write, or calendar update/delete.</li> <li>Guardrail depends on Ollama (verified): without it every guarded run pauses for approval. Intentional   fail-safe, but an outage halts autonomy.</li> <li>Data leaves the machine (verified in CLI docs): prompts and observations go to the configured LLM provider,   and connectors call external APIs.</li> <li>One active CLI run per profile (verified in CLI docs); no daemon, background execution, <code>serve</code>, or resume.</li> <li>Detector quality (unverified): accuracy of the six LLM detectors on <code>qwen2.5:7b</code> has not been measured in   the docs reviewed; there are no evaluation results or benchmark reports in the repo.</li> <li>Live provider behaviour (unverified): connectors have unit tests with fakes; behaviour against real   Google, GitHub, Telegram, and Stripe accounts was not exercised while writing these docs.</li> <li>Benchmark endpoint (verified): <code>/benchmark</code> averages recorded latencies only. There is no raw-vs-guarded   comparison harness in the repo.</li> </ul>"},{"location":"limitations.html#testing","title":"Testing","text":"<ul> <li><code>tests/e2e/</code> and <code>tests/fixtures/</code> are empty placeholders.</li> <li>No CI configuration was found in the repository.</li> <li>Migrations are not exercised against PostgreSQL by the default test suite.</li> </ul>"},{"location":"security.html","title":"Security and threat model","text":"<p>AgentGate is a control point between an untrusted, non-deterministic planner (an LLM that can be manipulated by content it reads) and real systems (mail, calendar, code, money, browsers). This page states what it defends, where the trust boundaries are, and what it does not cover.</p>"},{"location":"security.html#assets","title":"Assets","text":"Asset Where it lives Provider credentials (OAuth tokens, bot token, Stripe key, LLM key) Keychain / env (CLI); <code>.env</code> and <code>oauth_tokens</code> table (server) User data reachable through connectors Gmail, Calendar, GitHub, local files, web pages Money movement Stripe Checkout and refunds The audit trail <code>audit_logs</code>, <code>guardrail.jsonl</code>, SQLite"},{"location":"security.html#trust-boundaries","title":"Trust boundaries","text":"<pre><code>flowchart LR\n    U[User / channel] --&gt;|prompt| P[LLM planner - untrusted]\n    W[Web pages, emails, files] --&gt;|content| P\n    P --&gt;|ActionRequest| G[Guardrail + host rules - trusted]\n    G --&gt;|approved / allowed| X[Executors + connectors]\n    X --&gt; S[External systems]\n    G --&gt; A[(Audit)]</code></pre> <p>Treat everything left of the guardrail as untrusted, including the planner's <code>risk_hint</code>, <code>domain</code>, and <code>confidence</code>.</p>"},{"location":"security.html#threats-and-mitigations","title":"Threats and mitigations","text":"Threat Mitigation Residual risk Prompt injection from page/email/file content steering the planner Prompt-injection detector (BLOCK); host allowlist of operations; approval for external effects Detector is an LLM: it can miss novel attacks Planner lies about risk (wrong <code>risk_hint</code>/<code>domain</code>) Trusted per-operation hints; domain fallback by target system; hints can only raise the floor Operations not yet classified default to policy rules Unknown or new operation Unregistered operations are <code>BLOCK</code> None by design Secret exfiltration (key in an outbound payload) Secret patterns and detector; <code>BLOCK</code> on egress; secrets masked before reaching Ollama Unusual secret formats may not match PII leakage in external sends PII policy: <code>SANITIZE</code> (redact) or approval; bulk PII egress <code>BLOCK</code> Detector accuracy Destructive/irreversible action Approval; higher risk without rollback Human approver fatigue Unauthorized payments / refunds Server-side price allowlist, quantity cap, approval, test-mode first, signed webhooks Live keys widen blast radius Path traversal / arbitrary file read Resolved-path allowlist Misconfigured <code>ALLOWED_FILESYSTEM_PATHS</code> Detector outage Fail closed: approval required; failed evaluation persistence blocks execution Availability: autonomy stops Tampering with audit DB trigger blocks UPDATE/DELETE; CLI SQLite triggers A DB owner or superuser can drop triggers or the table Forged webhooks Stripe HMAC + tolerance; Telegram secret header Depends on secret strength Wrong recipient Telegram names resolved to numeric IDs before approval, shown in the approval view Contact registry is populated from inbound messages Approval bypass in automation CLI: no <code>--yes</code>; non-interactive runs exit 3 Server approval endpoint has no authentication (below)"},{"location":"security.html#known-gaps-fix-before-exposing-publicly","title":"Known gaps (fix before exposing publicly)","text":"<ol> <li>No API authentication or authorization. Any client that can reach the server can start runs and approve    or decline steps. Put it behind an authenticating gateway or bind it to localhost.</li> <li>CORS is <code>*</code>. Acceptable for a local demo, not for a shared deployment.</li> <li>OAuth tokens are stored unencrypted in <code>oauth_tokens</code> (<code>access_token</code>, <code>refresh_token</code>) in server mode.    ADR 0002 identifies this as a server-side seam: the CLI keeps credentials in the OS keychain and its SQLite    holds only references and scope/expiry metadata, never silently falling back to plaintext. For the server, use    disk/database encryption and restrict DB access, or add application-level encryption.</li> <li>OAuth <code>state</code> is kept in process memory (<code>_pending_states</code>), so authorization must complete on the same    process, and multi-worker deployments will fail callbacks.</li> <li>Live run state is in memory. Approvals pending at restart are lost; the audit row remains as    <code>PENDING_APPROVAL</code>.</li> <li>Tracked env templates. <code>.env.development</code> and <code>.env.staging</code> are committed; never put real secrets in them.</li> <li>Sensitive data still goes to the LLM provider. Prompts and observations reach the configured planner.    The CLI masks known credential values from outgoing context; the coverage is patterns, not every secret.</li> <li>Data retention is unenforced. <code>*_RETENTION_DAYS</code> settings exist, but no cleanup job was found.</li> <li>Approval is human-judgment. The approval view shows action, target, payload, risk, and reasons; reviewers    still need to read it.</li> <li>Audit is not tamper-proof against the operator (stated in the CLI docs). Ship logs to an external,     append-only sink if you need that.</li> </ol>"},{"location":"security.html#hardening-checklist","title":"Hardening checklist","text":"<ul> <li>[ ] Run behind an authenticating reverse proxy; restrict CORS origins.</li> <li>[ ] Use least-privilege credentials: read-only tokens where possible, Stripe test keys until reviewed,       Telegram bot with minimal chat access.</li> <li>[ ] Keep <code>GUARDRAIL_BACKEND=agentgate</code>; monitor Ollama health.</li> <li>[ ] Set <code>ALLOWED_FILESYSTEM_PATHS</code> and <code>LOCAL_FILE_ROOT</code> to the narrowest directories.</li> <li>[ ] Encrypt the database volume and backups; restrict who can connect.</li> <li>[ ] Rotate provider secrets; store them in a secret manager, not tracked files.</li> <li>[ ] Set webhook secrets; rotate them if leaked.</li> <li>[ ] Back up and forward audit tables; alert on <code>BLOCKED</code> and <code>FAILED</code> spikes.</li> <li>[ ] Review policy pack rules and hint lists before adding new connectors.</li> <li>[ ] Use Google OAuth test users / limited scopes; revoke tokens on decommission (<code>agentgate disconnect</code>).</li> </ul>"},{"location":"security.html#reporting","title":"Reporting","text":"<p>No security contact is defined in the repository. Add a <code>SECURITY.md</code> with a contact and disclosure process before making the project public.</p>"},{"location":"service-contracts.html","title":"AgentGate Service Contracts","text":""},{"location":"service-contracts.html#runtime-chain","title":"Runtime Chain","text":"<p>Every guarded action produces:</p> <pre><code>raw_tool_call -&gt; ActionRequest -&gt; DecisionResponse -&gt; ExecutionResult -&gt; AuditEvent -&gt; ActionTrace\n</code></pre> <p>All public records include <code>schema_version</code>, <code>run_id</code>, and <code>action_id</code> where applicable.</p>"},{"location":"service-contracts.html#endpoints","title":"Endpoints","text":"Method Path Purpose GET <code>/api/v1/runs</code> List runs with action counts and latest status GET <code>/api/v1/runs/{run_id}/actions</code> List audit events for one run GET <code>/api/v1/actions/{action_id}</code> Inspect one action chain GET <code>/api/v1/audits</code> List audit events, optional <code>run_id</code> filter GET <code>/api/v1/audits/latest</code> Return latest audit event GET <code>/api/v1/approvals</code> Return pending approval events GET <code>/api/v1/benchmark</code> Return current latency summary"},{"location":"service-contracts.html#cli","title":"CLI","text":"<pre><code>python scripts/run_demo_scenario.py local_file_read\npython scripts/run_demo_scenario.py browser_snapshot\npython scripts/export_audit.py --latest\npython scripts/export_traces.py --latest\n</code></pre> <p>Audit JSONL defaults to <code>artifacts/audit/events.jsonl</code>. Trace JSONL defaults to <code>artifacts/traces/actions.jsonl</code>.</p>"},{"location":"testing.html","title":"Testing","text":"<p>About 350 test functions across unit, integration, and vendored-guardrail suites.</p>"},{"location":"testing.html#run","title":"Run","text":"<pre><code>pip install -e \".[dev,server,browser,stripe]\"\npytest                          # everything\npytest tests/unit               # fast, no external services\npytest tests/integration\npytest tests/upstream_guardrail # tests of the embedded guardrail engine\npytest -k calendar -x           # by keyword, stop at first failure\n</code></pre> <p>Lint and types:</p> <pre><code>ruff check .\nblack --check .\nmypy app\n</code></pre>"},{"location":"testing.html#layout","title":"Layout","text":"Path Covers <code>tests/test_health.py</code> Health and root endpoints <code>tests/unit/</code> Agent loop, decision flows, guardrail (hybrid, sensitive, diagnostics), schemas, LLM client and parser, run registry/service, connectors (GitHub, Gmail, Calendar, Telegram, Stripe), OAuth, audit repository, DB session, CLI, credentials, local browser and payments <code>tests/integration/</code> End-to-end flows: local file and browser demo paths, Sprint 2 endpoints and traces, chat agent endpoints, calendar flow, Telegram and Stripe webhooks, agentgate guardrail <code>tests/upstream_guardrail/</code> Vendored engine: text scanning, fail-closed detectors, decision concurrency; uses <code>fake_llm.py</code> and <code>fake_audit.py</code> <code>tests/e2e/</code>, <code>tests/fixtures/</code> Empty placeholders"},{"location":"testing.html#isolation","title":"Isolation","text":"<p><code>tests/conftest.py</code> applies an autouse fixture that makes tests independent of your machine:</p> <ul> <li><code>GUARDRAIL_BACKEND=legacy</code> and <code>GUARDRAIL_LLM_ENABLED=false</code> (no Ollama or LLM needed)</li> <li><code>AUDIT_BACKEND=jsonl</code> with audit files in <code>tmp_path</code></li> <li><code>LLM_TYPE=openai</code>, <code>ATOMIC_BROWSER_AUDIT=false</code></li> <li><code>get_settings.cache_clear()</code> before and after</li> </ul> <p>Tests that exercise the embedded engine select <code>agentgate</code> explicitly and fake only the Ollama transport.</p>"},{"location":"testing.html#writing-tests","title":"Writing tests","text":"<ul> <li>Call <code>get_settings.cache_clear()</code> after changing environment variables with <code>monkeypatch</code>.</li> <li>Use <code>tmp_path</code> for audit, trace, and <code>LOCAL_FILE_ROOT</code> so tests never touch real data.</li> <li>Fake external systems at the <code>httpx</code> client boundary; connectors accept an injected <code>httpx.AsyncClient</code>.</li> <li>Add a test for every behaviour change; put connector tests next to the existing <code>test_&lt;name&gt;_connector.py</code>.</li> <li>For new guardrail rules, cover BLOCK, NEED_APPROVAL, SANITIZE, and ALLOW paths and the fail-closed case.</li> </ul>"},{"location":"testing.html#what-is-not-tested-here","title":"What is not tested here","text":"<p>Live provider calls (Gmail, GitHub, Calendar, Stripe, Telegram), a real PostgreSQL migration run in CI, and a real Ollama detector are not part of the default suite. Verify those manually with the setup steps in getting-started and connectors.</p>"},{"location":"decisions/0001-architecture-foundation.html","title":"ADR 0001 \u2014 Architecture Foundation","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-06</li> <li>Context: Sprint 0 \u2014 Data Engineering Foundation</li> </ul>"},{"location":"decisions/0001-architecture-foundation.html#decision","title":"Decision","text":"Area Decision Architecture Style Modular Monolith Backend Framework FastAPI Language Python 3.11 Database PostgreSQL 17 (JSONB for semi-structured payloads) ORM SQLAlchemy 2.0 Migration Tool Alembic Browser Automation Playwright Queue Layer Redis (future-ready) Containerization Docker &amp; Docker Compose Secrets (MVP) <code>.env</code> via pydantic-settings Secrets (Prod) AWS Secrets Manager / HashiCorp Vault"},{"location":"decisions/0001-architecture-foundation.html#rationale","title":"Rationale","text":"<p>A Modular Monolith keeps the MVP simple while preserving clean domain boundaries (agent, guardrail, approval, audit, connector, browser, benchmark) so that <code>audit</code>, <code>approval</code>, <code>connector</code>, and <code>benchmark</code> can later be extracted into independent services. <code>agent</code>, <code>guardrail</code>, <code>browser</code>, and <code>llm</code> stay Python-coupled to the AI tooling and Playwright ecosystem.</p>"},{"location":"decisions/0001-architecture-foundation.html#consequences","title":"Consequences","text":"<ul> <li>Single deployable unit for MVP; horizontal scaling later via Redis queue + workers.</li> <li>All connectors implement a shared <code>BaseConnector.execute(action, payload)</code> contract.</li> <li>Logs are structured JSON for observability, auditability, and benchmark analysis.</li> </ul>"},{"location":"decisions/0002-local-first-cli.html","title":"ADR 0002: Local-first CLI runtime","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-09-13</li> <li>Scope: Local foreground CLI. See the implemented command guide.</li> <li>Relationship: Keeps ADR 0001's modular monolith and domain boundaries. Introduces   a local CLI as the first packaged runtime, with the HTTP server as a later adapter.</li> </ul>"},{"location":"decisions/0002-local-first-cli.html#decision","title":"Decision","text":"<p>Package AgentGate as an installable Python command that runs the existing agent engine directly in the terminal process. Ordinary runs do not start FastAPI, require Docker, connect to PostgreSQL or Redis, or call a localhost application API.</p> <p>The first release targets one user, one profile, and one foreground run at a time. Local-first means execution, approvals, credentials, and history are owned by the user's machine. It does not mean offline: configured LLMs and external connectors still make outbound network requests. Prompts and observations sent to an LLM leave the machine, so users must be told what each integration shares.</p> <pre><code>Terminal user\n    |\nCLI adapter: arguments, rendering, approval and secret input\n    |\nAgent application service: submit / events / respond / cancel / history\n    |\nLocalRuntime: constructs dependencies and owns their lifecycle\n    |\nExisting core: plan -&gt; guardrail -&gt; approve or clarify -&gt; execute -&gt; observe\n                   ^                                       |\n                   +--------------- replan ----------------+\n    |\n    +-- Connectors: local files, browser, GitHub, Gmail, Calendar, Stripe\n    +-- SQLite: run history, safe events, audit, execution intents, payment state\n    +-- OS keychain: API keys and OAuth access/refresh tokens\n    +-- User config: non-secret settings and enabled capabilities\n\nLater: an HTTP adapter calls the same application service with a ServerRuntime.\n</code></pre>"},{"location":"decisions/0002-local-first-cli.html#what-can-be-reused-and-what-must-change","title":"What can be reused, and what must change","text":"<p>The existing <code>start_agent_run</code> service already launches an asynchronous run without HTTP. <code>RunSession.events</code> provides events, and <code>RunRegistry.respond</code> resolves approval/input waiters. Keep these behaviors and the planner, guardrail, connector contracts, and run-scoped browser sessions. The CLI is another interaction adapter, not a second agent implementation.</p> <p>The implementation addressed these original packaging seams:</p> <ul> <li><code>app/database/session.py</code> constructs a PostgreSQL engine at import time and   normalizes URLs for asyncpg. A SQLite URL alone will not make it a local runtime.</li> <li>The audit repository selector eagerly imports its database implementation.   Selecting JSONL does not remove database dependencies from OAuth or Stripe.</li> <li>OAuth repositories depend on global <code>SessionLocal</code> and currently store token   values in database rows. Local credentials need a separate secret store.</li> <li><code>app/executors/api_executor.py</code> eagerly imports and constructs every connector.   Optional integrations need lazy registration and injected dependencies.</li> <li>Settings mix core behavior with server URLs and infrastructure. Local runs must   not require server settings, or use nonexistent localhost checkout landing pages.</li> <li>Migration <code>0001</code> contains PostgreSQL JSONB and trigger SQL. Preserve the existing   migration history; introduce separately tested local schema migrations.</li> <li><code>pyproject.toml</code> has no console entry point and includes server dependencies in   the base installation. Installed execution must also stop relying on repository   paths for assets, data, or config.</li> </ul>"},{"location":"decisions/0002-local-first-cli.html#application-boundary-and-composition","title":"Application boundary and composition","text":"<p>Introduce a small application facade exposing run submission, event observation, responses, cancellation, and historical queries. Requests and safe event DTOs must not contain FastAPI objects or terminal rendering concerns. Responses identify the run and exact pending action; changed actions require new approval.</p> <p><code>LocalRuntime</code> injects validated settings, the planner, guardrail, executor/connector registry, storage, credentials, and interaction/event services. It initializes and closes resources explicitly. Importing core modules must not open databases, launch browsers, validate unrelated credentials, or import optional server SDKs.</p> <p>Use narrow repository contracts for run history, audit, execution intents, OAuth metadata, and payments. Keep the existing <code>BaseConnector</code> execution contract. Do not introduce a generic plugin platform or remote transport in the first version.</p> <p>Implemented additions, alongside the existing domain packages:</p> <ul> <li><code>app/cli/</code>: entry point, commands, terminal interaction, safe output renderer.</li> <li><code>app/runtime/</code>: dependency contracts and local composition/lifecycle.</li> <li><code>app/storage/local/</code>: SQLite repositories and versioned local migrations.</li> <li><code>app/credentials/</code>: OS keychain and explicit environment/session providers.</li> </ul> <p>Existing HTTP routes stay intact during extraction. A future <code>ServerRuntime</code> can select PostgreSQL and server-managed credentials. Authentication, ownership, durable workers, event fan-out, and reconnect semantics remain server-specific work, not something dependency injection solves automatically.</p>"},{"location":"decisions/0002-local-first-cli.html#foreground-execution-lifecycle","title":"Foreground execution lifecycle","text":"<ol> <li>Parse arguments before initializing the runtime. <code>--help</code> needs no credentials,    database, browser, or network connection.</li> <li>Resolve trusted configuration and enabled capabilities. Initialize local schema    and acquire a per-profile execution lock; reject a second active run in v1.</li> <li>Create and persist the run ID. Start the engine and consume its events within    the same asynchronous application lifetime.</li> <li>Display safe progress. For approvals, show the exact action, target, consequence,    and financial details when relevant. For clarification, collect requested fields.    Use hidden input for secrets and pass responses to the engine's waiters.</li> <li>Persist safe events and action outcomes. Await the engine task as well as the    event consumer so exceptions, timeouts, and cancellation cannot strand the CLI.</li> <li>On completion or Ctrl+C, finalize state, flush storage, close browser/HTTP/database    resources, and release the lock. Do not claim an in-flight remote action was    undone merely because local execution was cancelled.</li> </ol> <p>The current queue is a single-consumer queue, not a broadcast event bus. The CLI owns one consumer; persistence happens at a shared publication boundary rather than through a second consumer competing for the same queue. A safe event envelope includes schema version, run ID, sequence, type, and timestamp.</p> <p>Active tasks remain process-bound. On the next startup, historical runs left active after a crash become interrupted. History is not automatic workflow resumption. Never replay a payment, message, or calendar write solely from an unfinished run.</p>"},{"location":"decisions/0002-local-first-cli.html#local-state-and-security","title":"Local state and security","text":"<p>Use platform-appropriate per-user directories, independent of the working directory and installed package location. On Linux the layout is:</p> <ul> <li><code>$XDG_CONFIG_HOME/agentgate/config.toml</code>, default <code>~/.config/agentgate/config.toml</code>.</li> <li><code>$XDG_DATA_HOME/agentgate/state.sqlite3</code>, default <code>~/.local/share/agentgate/state.sqlite3</code>.</li> <li>Private artifacts beneath the data directory. Automated retention is deferred.</li> </ul> <p>SQLite stores non-secret run summaries, sanitized event/audit records, connector metadata, remote IDs, payment observations with checked-at times, and execution intents. Preserve audit's final write-once-per-action contract; use a separate intent journal to record an approved operation before its external effect and its outcome afterward. If durable intent recording fails, do not perform a write.</p> <p>Persist stable operation identity and idempotency data before financial requests. The intent journal must not contain customer email, passwords, tokens, or raw browser input. An ambiguous remote outcome stays unknown until reconciled. Local storage is not tamper-proof against the machine owner and does not provide distributed exactly-once execution.</p> <p>Store credentials in an available secure OS keychain. SQLite contains references and token scope/expiry metadata, not credential values. If no secure backend exists, allow an explicitly chosen environment or session-only provider; never silently fall back to plaintext. Do not accept secret values as command-line arguments.</p> <p>Apply sanitization consistently before events, traces, audit, prompts-to-history, and logs are persisted or rendered. Existing masking is a starting point, not a guarantee that every arbitrary prompt, connector error, or artifact is safe.</p> <p>Configuration precedence: command options, supported environment overrides, an explicitly selected config file (or the user config), defaults. Project configuration is non-secret and cannot lower trusted guardrail rules or silently expand filesystem access. Require an explicit allowed workspace root and validate resolved paths, including symlinks. Browser cookies are ephemeral in v1; persistent profiles are deferred.</p>"},{"location":"decisions/0002-local-first-cli.html#cli-surface","title":"CLI surface","text":"<p>Initial commands:</p> <pre><code>agentgate init\nagentgate doctor\nagentgate connect github\nagentgate connect gmail\nagentgate connect calendar\nagentgate connect stripe\nagentgate disconnect calendar\nagentgate setup browser\nagentgate run \"Read today's calendar and summarize it\"\nagentgate history\nagentgate show &lt;run-id&gt;\nagentgate payments sync\n</code></pre> <p><code>init</code> writes non-secret configuration and initializes local storage. <code>doctor</code> performs safe diagnostics without displaying credentials. <code>connect stripe</code> collects a user-supplied test-mode key through hidden input; it is not Stripe Connect OAuth.</p> <p>Human output is the default. <code>run --json --non-interactive</code> emits versioned, sanitized NDJSON on stdout and diagnostics on stderr. Required approval or input fails closed with an interaction-required result; there is no global <code>--yes</code> guardrail bypass. Exit codes: 0 success, 1 failure/blocked/declined, 2 invalid usage/configuration, 3 interaction required, 130 user interruption.</p> <p>No <code>serve</code>, background-run, cross-process <code>respond</code>, or automatic <code>resume</code> commands in v1. After the command exits, <code>show</code> reads history, not a live engine.</p>"},{"location":"decisions/0002-local-first-cli.html#integrations-without-an-application-server","title":"Integrations without an application server","text":""},{"location":"decisions/0002-local-first-cli.html#oauth","title":"OAuth","text":"<p>Use the system browser and a temporary listener bound only to a loopback IP for the authorization callback. Verify state, expire pending requests, close the listener after completion, and use PKCE for supported native flows. This short-lived callback receiver is not a persistent AgentGate API server. Google supports this flow for Desktop app registrations. See Google's installed-app guide and native OAuth security guidance.</p> <p>Do not ship confidential web-app secrets as if a distributed CLI could protect them. Handle registration and supported authorization flow per provider. Start GitHub with user-supplied tokens if an appropriate native authorization flow is not configured. Request only required scopes and expose disconnect/revocation.</p>"},{"location":"decisions/0002-local-first-cli.html#stripe","title":"Stripe","text":"<p>The local release supports guarded test-mode checkout/refund requests and explicit status reconciliation, not an unattended payment fulfillment service. Keep the catalog allowlist, trusted financial risk classification, and approval requirements. Open hosted Checkout in the user's browser so card details stay outside AgentGate.</p> <p>Use configured valid success/cancel URLs; require configuration instead of opening the current nonexistent localhost landing pages. A redirect is never proof of payment. <code>payments sync</code> retrieves tracked sessions/refunds and saves the observed provider status and timestamp. Bounded opt-in polling may help an active user, but nothing updates while the CLI is closed. Session completion and paid status are distinct. See Checkout Session retrieval.</p> <p>Keep retries tied to the persisted logical operation and unchanged request inputs, using Stripe idempotency. Do not assume an old key protects a retry indefinitely; reconcile unknown outcomes before allowing another financial attempt.</p> <p>Polling is a limited local status tool, not a replacement for production webhooks. Reliable automated fulfillment and asynchronous payment handling belong to the future server's signed webhook/reconciliation path, as explained in Stripe's fulfillment guide.</p>"},{"location":"decisions/0002-local-first-cli.html#other-channels","title":"Other channels","text":"<p>GitHub, Gmail, Calendar, local file access, and browser automation can run through outbound calls or local execution. Telegram outbound actions may use the same connector, but its inbound webhook channel is outside the first local release.</p>"},{"location":"decisions/0002-local-first-cli.html#packaging-and-delivery","title":"Packaging and delivery","text":"<p>Keep Python 3.11+ and the existing <code>app</code> import package initially. Use standard-library argument parsing for the first CLI. Entry point:</p> <pre><code>[project.scripts]\nagentgate = \"app.cli.main:main\"\n</code></pre> <p>Build a wheel installable from source or a release artifact with pipx. Publication to a package index and a standalone executable are separate future decisions. See the Python CLI packaging guide.</p> <p>Keep core, SQLite, configuration, HTTP client, and secure credential support in the base installation. Move Playwright to a <code>browser</code> extra, Stripe to a <code>stripe</code> extra, and FastAPI/Uvicorn/PostgreSQL drivers/Redis to a <code>server</code> extra. Extras only work after eager imports are removed. Browser installation is an explicit setup step, never a surprise download during <code>--help</code> or the first run.</p> <p>Implement in vertical slices:</p> <ol> <li>Extract runtime dependencies and event/interaction seams while keeping HTTP    behavior working. Add no-server import smoke tests.</li> <li>Add SQLite migrations/repositories, private data paths, credentials, and intent    persistence. Prove a local file run requires no PostgreSQL or Redis.</li> <li>Add the entry point, foreground event rendering, approvals, clarification,    cancellation, history, and non-interactive behavior.</li> <li>Add desktop OAuth, browser bootstrap, and test-mode Stripe reconciliation.</li> <li>Build and install a wheel in a clean environment outside the repository;    document setup, supported capabilities, and deferred server features.</li> </ol> <p>Acceptance: fresh-install <code>--help</code> works with no provider configuration; a file run works without external infrastructure; approvals remain action-specific; secrets never enter history; cancellation cleans up; crash recovery never auto-replays writes; historical records survive exit; optional integrations fail with actionable setup messages; and the local schema upgrades without running PostgreSQL SQL.</p>"},{"location":"decisions/0002-local-first-cli.html#deferred-server-development","title":"Deferred server development","text":"<p>Add an HTTP adapter and ServerRuntime when remote/multi-user execution is needed. Reuse the application service, domain policies, connector contracts, and safe event schema. Add server authentication and ownership, durable workflow/approval state, workers, broadcast/replayable events, managed secrets, and inbound webhooks then.</p> <p>Do not build those systems into the local MVP, and do not promise local-to-server database synchronization or seamless workflow migration in this architecture.</p>"},{"location":"decisions/0003-embedded-guardrail-engine.html","title":"ADR 0003: Embed the upstream guardrail core","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-09-17</li> </ul>"},{"location":"decisions/0003-embedded-guardrail-engine.html#context","title":"Context","text":"<p>The application has the local CLI, API, connectors, interaction flow, and durable history. NafisNaufal/agentgate has six Ollama detectors, validated declarative policies, risk scoring, and sanitization. Their schemas and CLI/package names collide, and upstream's PostgreSQL audit default conflicts with ADR 0002.</p>"},{"location":"decisions/0003-embedded-guardrail-engine.html#decision","title":"Decision","text":"<p>Import the core at revision <code>9b919c604d2a0955fff26b4f05045281514c0220</code> into a private vendored namespace. Adapt the application action contract and trusted connector metadata to the engine, then map its result back with existing policy floors. Select it by default through the existing sync/async decision entry points. Retain <code>GUARDRAIL_BACKEND=legacy</code> as an explicit compatibility option.</p> <p>Inject a durable evaluation journal into the engine. Every decision is recorded before it reaches execution, separately from the existing final action audit. Reuse the current planner, approval loop, and executors. Re-evaluate structured redactions and clarified actions before execution.</p>"},{"location":"decisions/0003-embedded-guardrail-engine.html#consequences","title":"Consequences","text":"<p>Guarded runs now require Ollama; detector failure holds actions for approval. No new Python dependencies, console command, PostgreSQL requirement for the CLI, or database migrations are introduced. Server deployments must persist the evaluation journal. The embedded source and host adapter can be reviewed and updated independently. See the integration guide for scope, configuration, and verification.</p>"},{"location":"decisions/0004-audit-granularity.html","title":"ADR 0004: Audit granularity and event sourcing","text":"<ul> <li>Status: Proposed (needs a team decision)</li> <li>Context: Sprint 2 audit design v4 vs. the shipped implementation</li> </ul>"},{"location":"decisions/0004-audit-granularity.html#context","title":"Context","text":"<p>The shipped audit is action-sourced: one immutable <code>audit_logs</code> row per action, <code>action_id</code> unique. Approval outcome is stored as <code>initial_decision</code> and <code>approval_decision</code>. A separate <code>guardrail.jsonl</code> journal records each guardrail evaluation. <code>ATOMIC_BROWSER_AUDIT</code> writes one row per browser step, otherwise one per batch.</p> <p>Spec v4 proposes an append-only <code>audit_events</code> stream with <code>sequence_no</code> and explicit events for approval and clarification. It improves replay, compliance metrics (F14/F16), and the approval timeline, but costs a new schema, a new repository, migration of readers (<code>/runs</code>, <code>/audits</code>, <code>/actions</code>, <code>/benchmark</code>, <code>/approvals</code>) and of the CLI's SQLite store.</p>"},{"location":"decisions/0004-audit-granularity.html#relationship-to-existing-adrs","title":"Relationship to existing ADRs","text":"<ul> <li>ADR 0002 (local-first CLI) deliberately preserves \"audit's final write-once-per-action contract\" and adds a   separate execution intent journal for approved operations (recorded before the external effect and again   with the outcome). Any move to an event stream amends that contract and should say so; the intent journal   already covers part of the timeline need for writes.</li> <li>ADR 0003 (embedded guardrail) added a per-evaluation guardrail journal that is separate from the final   audit row. Together with the intent journal it already captures evaluation and execution-intent history   outside <code>audit_logs</code>, so the question is narrower: whether the audit table itself should become an event stream.</li> </ul>"},{"location":"decisions/0004-audit-granularity.html#options","title":"Options","text":"<ol> <li>Keep action-sourced (consistent with ADR 0002). Simple, already works with every reader, with the guardrail    and intent journals covering evaluation and write-intent history. Weak on approval-request timing, ordering, replay.</li> <li>Add <code>audit_events</code> alongside <code>audit_logs</code>. Write events during the loop; keep <code>audit_logs</code> as the    summary. Readers migrate gradually. Two writes per action.</li> <li>Replace <code>audit_logs</code> with <code>audit_events</code>. Cleanest model, largest migration; must rebuild derived views.</li> </ol>"},{"location":"decisions/0004-audit-granularity.html#decision-drivers","title":"Decision drivers","text":"<ul> <li>Need to show when approval was requested and decided, and by whom.</li> <li>Approval pending at restart currently leaves no durable trace in chat runs.</li> <li>Immutability trigger must be carried to any new table.</li> <li>The unique <code>action_id</code> constraint blocks multiple rows per action.</li> </ul>"},{"location":"decisions/0004-audit-granularity.html#suggested-direction","title":"Suggested direction","text":"<p>Option 2: introduce <code>audit_events</code> with the same immutability trigger, emit events at approval, clarification, sanitize, execution start/finish, and derive current state by fold. Keep <code>audit_logs</code> as a compatibility summary until readers move. Decide <code>written_by</code> and <code>session_id</code> semantics (CLI has runs but no login/session concept).</p>"},{"location":"decisions/0004-audit-granularity.html#consequences","title":"Consequences","text":"<p>Needs a migration <code>0005</code>, a repository with the same async interface plus <code>append_event</code>, tests for replay ordering, and updated docs. Record the final choice here and change the status to Accepted.</p>"},{"location":"integrations/guardrail.html","title":"Merged AgentGate guardrail","text":"<p>This project embeds the guardrail core from NafisNaufal/agentgate, revision <code>9b919c604d2a0955fff26b4f05045281514c0220</code>, version 0.3.0. The current CLI, FastAPI routes, planner, connectors, approvals, and final audit stores remain the application runtime. The embedded code is under <code>app/domains/guardrail/_vendor/agentgate</code>; provenance is recorded in its <code>NOTICE.md</code>.</p> <p>Both repositories publish a distribution and console command named <code>agentgate</code>. Embedding the pinned core avoids a self-dependency or replacing this project's CLI. Install only this project; do not install the other distribution alongside it.</p>"},{"location":"integrations/guardrail.html#setup","title":"Setup","text":"<p>The default backend is <code>GUARDRAIL_BACKEND=agentgate</code>. Its detector is independent of the planner provider/API key. Install Ollama, then:</p> <pre><code>ollama pull qwen2.5:7b\n$env:OLLAMA_NUM_PARALLEL = \"6\"\nollama serve\n</code></pre> <p>If Ollama already runs, set parallelism in its service configuration instead of starting a second process. POSIX equivalent: <code>OLLAMA_NUM_PARALLEL=6 ollama serve</code>. The engine dispatches six requests concurrently, but server capacity determines whether inference actually overlaps.</p> Setting Default Purpose <code>GUARDRAIL_BACKEND</code> <code>agentgate</code> <code>agentgate</code> or explicit <code>legacy</code> rollback <code>OLLAMA_HOST</code> <code>http://localhost:11434</code> (<code>http://host.docker.internal:11434</code> in Docker Desktop) Detector endpoint; remote hosts require HTTPS <code>AGENTGATE_LLM_DETECTOR_MODEL</code> <code>qwen2.5:7b</code> Local detector model <code>AGENTGATE_LLM_DETECTOR_TIMEOUT</code> <code>30</code> Seconds per detector request; no retries <code>AGENTGATE_DETECTOR_ARCHITECTURE</code> <code>six</code> Six detectors or upstream experimental <code>unified</code> <code>GUARDRAIL_AUDIT_PATH</code> <code>artifacts/audit/guardrail.jsonl</code> Server evaluation journal <p>The Compose setup points the API container at <code>http://host.docker.internal:11434</code> when Ollama is running on the Docker Desktop host. This reserved local bridge hostname is allowed for HTTP; arbitrary remote HTTP detector endpoints remain rejected and must use HTTPS.</p> <p>The server reads these from its normal settings/<code>.env</code>. The CLI reads supported shell overrides and always writes its evaluation journal to <code>&lt;AGENTGATE_DATA_DIR&gt;/guardrail.jsonl</code> (or the platform's default private data directory). It does not load project <code>.env</code> files. <code>agentgate doctor</code> checks the Ollama model list without generating text, pulling models, or executing actions.</p> <p>Ordinary setup/help/history operations need no Ollama. A run whose detector is unavailable pauses for approval; non-interactive runs return interaction-required status. The integration does not silently permit actions or select the old backend.</p>"},{"location":"integrations/guardrail.html#evaluation-and-enforcement","title":"Evaluation and enforcement","text":"<pre><code>Existing planner / API proposal\n  -&gt; application ActionRequest and trusted connector metadata\n  -&gt; upstream PII / secrets / source code / payment-phishing /\n     prompt-injection / action-intent detectors\n  -&gt; upstream JSON policy packs and risk scoring\n  -&gt; existing application policy floor and structured redaction\n  -&gt; durable evaluation journal\n  -&gt; existing approval / clarification / executor flow\n  -&gt; existing final action audit and trace\n</code></pre> <p>The adapter scans the complete payload, including nested values, even when the planner supplies a harmless summary. It translates <code>booking</code> to <code>booking_style</code>, connector names to upstream target-system names, and single risk hints to lists. Trusted connector operations supply additional risk and rollback metadata; unknown operations and incompatible action types are blocked. Existing rules for payments, destructive actions, and required Calendar fields remain enforced.</p> <p>Both synchronous <code>decide</code> and asynchronous <code>adecide</code> use the selected backend. The async entry point runs the blocking upstream engine in a worker thread so detector calls do not block CLI events or the API event loop. Legacy browser prototype batches are mapped to their most consequential action type.</p> <p>The five decision values retain their meanings:</p> Decision Application behavior <code>ALLOW</code> Execute through the existing router <code>BLOCK</code> Do not execute; cannot be approved around <code>NEED_APPROVAL</code> Wait for approval of the exact step <code>ASK_USER</code> Collect clarification and re-evaluate; clarification is not approval <code>SANITIZE</code> Substitute permitted content fields, re-evaluate, and request confirmation <p>Structured redaction walks content values without changing IDs, destinations, paths, amounts, or field types. Compound browser fills and selects are rewritten inside their validated action list. Secret material that cannot be rewritten into a supported content field is blocked, so later approval cannot release its original value. The router rejects a SANITIZE decision with no replacement payload. Detector prompts redact recognized credential fields and patterns before the Ollama request; the deterministic host floor preserves the corresponding secret-egress block. A detector error holds the action without execution while preserving any successful detector's BLOCK finding. Required clarification remains a prerequisite; after it is supplied, a detector error requires approval.</p>"},{"location":"integrations/guardrail.html#audit-integration","title":"Audit integration","text":"<p>Upstream requires an audit write before returning a decision. The injected host store meets that requirement using a locked, flushed, fsynced JSONL evaluation journal instead of introducing a second PostgreSQL schema. This preserves the local CLI's independence from PostgreSQL. Mount/persist the configured journal directory when deploying the server.</p> <p>Every evaluation records a unique ID, run/action IDs, timestamp, sanitized structured request, tool identity, and final mapped decision. Re-evaluation after input/redaction produces another entry for the same action. A failed write raises <code>AuditUnavailable</code> before execution. Final SQLite/PostgreSQL/JSONL action audits remain write-once and reference <code>guardrail_audit_id</code>; there is no database migration.</p> <p>This merge imports pre-action evaluation, not the upstream agent loop. Its task, terminal-message, and executor-observation screening stages are not transplanted; the existing runtime's observation/output sanitization still applies. Upstream executors, planner, scenario commands, and benchmarks are also outside this merge.</p>"},{"location":"integrations/guardrail.html#verification-and-maintenance","title":"Verification and maintenance","text":"<pre><code>pytest tests/integration/test_agentgate_guardrail.py tests/upstream_guardrail\npytest\n</code></pre> <p>Integration tests mock only detector transport and exercise real policy/scoring, redaction, failure handling, async dispatch, API/local runtime wiring, and durable audit behavior. Imported upstream tests cover scan text, concurrent detection, and malformed responses. Existing regression tests explicitly select the legacy backend and isolate settings from developer credentials.</p> <p><code>scripts/smoke_cli_package.py</code> tests a base-only wheel from outside the checkout, using local HTTP stubs for both planner and Ollama. It verifies bundled policies, doctor, a file run, evaluation/final audit linkage, history, and secret masking. It does not measure live model accuracy or latency.</p> <p>To update upstream, compare the files listed in <code>NOTICE.md</code> against a new pinned revision, retain the host audit shim, and rerun both suites and the wheel smoke. The vendored source is excluded from host formatting/linting and mypy errors so its contents stay comparable with the original source.</p>"},{"location":"integrations/stripe.html","title":"Stripe integration","text":"<p>AgentGate supports a deliberately small Stripe API surface. Financial writes pass through the normal action request, guardrail, approval, executor, and audit pipeline. Customers enter payment details only on Stripe-hosted Checkout.</p>"},{"location":"integrations/stripe.html#supported-actions","title":"Supported actions","text":"Action Required payload Behavior <code>create_checkout_session</code> <code>catalog_key</code>, optional <code>quantity</code> and <code>customer_email</code> Creates a one-time hosted Checkout Session <code>retrieve_checkout_session</code> <code>session_id</code> Retrieves safe checkout status fields <code>expire_checkout_session</code> <code>session_id</code> Expires an open session <code>create_refund</code> <code>payment_intent_id</code>, optional <code>amount</code> and <code>reason</code> Creates a full or partial refund <code>retrieve_refund</code> <code>refund_id</code> Retrieves safe refund status fields <p>There is no generic Stripe request action. Prices, currencies, redirect URLs, payouts, transfers, and arbitrary Stripe API paths cannot be supplied by the planner.</p>"},{"location":"integrations/stripe.html#configure-stripe","title":"Configure Stripe","text":"<p>Use Stripe sandbox/test mode first. Store real values in an ignored <code>.env</code> or a deployment secret manager, never in a tracked environment template.</p> <pre><code>STRIPE_SECRET_KEY=sk_test_...\nSTRIPE_WEBHOOK_SECRET=whsec_...\nSTRIPE_SUCCESS_URL=http://localhost:8000/payment/success\nSTRIPE_CANCEL_URL=http://localhost:8000/payment/cancel\nSTRIPE_PRICE_MAP={\"workshop_ticket\":\"price_...\",\"pro_plan\":\"price_...\"}\nSTRIPE_MAX_QUANTITY=20\nSTRIPE_WEBHOOK_TOLERANCE_SEC=300\n</code></pre> <p><code>STRIPE_PRICE_MAP</code> is the server-side allowlist. A prompt can select <code>workshop_ticket</code>, but only the server can resolve it to a real Stripe Price ID. Use a restricted key with only the permissions needed for Checkout Sessions and refunds when possible.</p> <p>Apply the database migration before receiving Stripe traffic:</p> <pre><code>uv run alembic upgrade head\n</code></pre> <p>The Stripe reconciliation tables are created by migration <code>0004</code>. If Checkout creation succeeds but the action reports <code>Stripe session was created but local reconciliation failed</code>, check the API log for a database error and run the migration against the same <code>DATABASE_URL</code> used by the API. Do not retry a live checkout until the migration has completed; retries within the same AgentGate run use a stable Stripe idempotency key and can safely recover the existing Checkout Session after persistence is restored.</p>"},{"location":"integrations/stripe.html#local-webhook-setup","title":"Local webhook setup","text":"<p>Install and authenticate the Stripe CLI, then forward sandbox events:</p> <pre><code>stripe listen --forward-to localhost:8000/api/v1/stripe/webhook\n</code></pre> <p>Copy the CLI-provided <code>whsec_...</code> value into <code>STRIPE_WEBHOOK_SECRET</code>. The secret for CLI forwarding is different from the secret assigned to a Dashboard webhook endpoint.</p> <p>Recommended event subscriptions:</p> <ul> <li><code>checkout.session.completed</code></li> <li><code>checkout.session.async_payment_succeeded</code></li> <li><code>checkout.session.async_payment_failed</code></li> <li><code>checkout.session.expired</code></li> <li><code>payment_intent.succeeded</code></li> <li><code>payment_intent.payment_failed</code></li> <li><code>charge.refunded</code></li> </ul> <p>The endpoint verifies <code>Stripe-Signature</code> against the untouched request body and persists each Stripe event ID once. Duplicate deliveries return a successful response without repeating state changes.</p>"},{"location":"integrations/stripe.html#planner-examples","title":"Planner examples","text":"<p>Create a hosted checkout:</p> <pre><code>{\n  \"action_type\": \"API_CALL\",\n  \"target_system\": \"stripe\",\n  \"domain\": \"booking\",\n  \"risk_hint\": \"payment\",\n  \"target\": \"workshop_ticket\",\n  \"payload\": {\n    \"action\": \"create_checkout_session\",\n    \"catalog_key\": \"workshop_ticket\",\n    \"quantity\": 1,\n    \"customer_email\": \"buyer@example.com\"\n  }\n}\n</code></pre> <p>Create a partial refund. <code>amount</code> is an integer in the currency's minor unit:</p> <pre><code>{\n  \"action_type\": \"API_CALL\",\n  \"target_system\": \"stripe\",\n  \"domain\": \"booking\",\n  \"risk_hint\": \"refund\",\n  \"target\": \"pi_...\",\n  \"payload\": {\n    \"action\": \"create_refund\",\n    \"payment_intent_id\": \"pi_...\",\n    \"amount\": 1250,\n    \"reason\": \"requested_by_customer\"\n  }\n}\n</code></pre> <p>AgentGate overwrites an incorrect Stripe domain or risk hint at the trusted <code>ActionRequest</code> boundary. Checkout creation, expiration, refunds, and the critical booking domain therefore cannot bypass approval by claiming a low-risk planner value.</p>"},{"location":"integrations/stripe.html#stored-data","title":"Stored data","text":"<p><code>stripe_payments</code> contains only Stripe object IDs, AgentGate run/action IDs, status, amount, currency, and timestamps. <code>stripe_webhook_events</code> contains only event IDs and processing metadata for deduplication. Full webhook payloads, card details, customer email addresses, client secrets, and API credentials are not stored in these tables.</p> <p>For production, set the Dashboard webhook endpoint to <code>https://your-host/api/v1/stripe/webhook</code>, match its API version to the pinned Stripe SDK release, use HTTPS, rotate keys, and monitor failed webhook delivery.</p>"},{"location":"integrations/telegram.html","title":"Telegram Integration","text":"<p>AgentGate supports Telegram as an inbound channel and outbound connector. Telegram-originated prompts use the same reactive agent loop, guardrails, approval flow, executor routing, and audit trail as <code>/api/v1/chat</code>.</p>"},{"location":"integrations/telegram.html#bot-setup","title":"Bot Setup","text":"<ol> <li>Create a bot with BotFather in Telegram.</li> <li>Copy the bot token into <code>.env</code> as <code>TELEGRAM_BOT_TOKEN</code>.</li> <li>Generate a long random webhook secret and set it as <code>TELEGRAM_WEBHOOK_SECRET</code>.</li> <li>Keep both values out of source control.</li> </ol> <p>Required environment:</p> <pre><code>TELEGRAM_BOT_TOKEN=\nTELEGRAM_WEBHOOK_SECRET=\nTELEGRAM_API_BASE=https://api.telegram.org\n</code></pre>"},{"location":"integrations/telegram.html#local-development","title":"Local Development","text":"<p>Telegram webhooks require a public HTTPS URL. For local development, run AgentGate and expose it with a tunnel such as ngrok, Cloudflare Tunnel, or another HTTPS reverse tunnel.</p> <p>Run AgentGate:</p> <pre><code>uvicorn app.main:app --reload\n</code></pre> <p>Public webhook endpoint:</p> <pre><code>https://&lt;public-host&gt;/api/v1/telegram/webhook\n</code></pre> <p>Register the webhook:</p> <pre><code>python scripts/set_telegram_webhook.py --url https://&lt;public-host&gt;/api/v1/telegram/webhook\n</code></pre> <p>The script sends Telegram the configured <code>TELEGRAM_WEBHOOK_SECRET</code> as the webhook <code>secret_token</code>. It does not print the bot token or secret.</p>"},{"location":"integrations/telegram.html#verification","title":"Verification","text":"<p>After registration, send the bot a Telegram text message such as:</p> <pre><code>Read sample.txt\n</code></pre> <p>Expected flow:</p> <pre><code>Telegram webhook\n-&gt; Telegram channel service\n-&gt; shared start_agent_run()\n-&gt; AgentGate reactive agent loop\n-&gt; guardrail\n-&gt; approval / sanitize / execute\n-&gt; existing executor architecture\n</code></pre> <p>The webhook returns quickly after creating the run. The bot sends an accepted message with the run id, then follows the run state without draining the SSE event queue used by browser clients.</p>"},{"location":"integrations/telegram.html#guardrails-and-approvals","title":"Guardrails And Approvals","text":"<p>Outbound Telegram actions are normal AgentGate connector actions:</p> <pre><code>{\n  \"action_type\": \"API_CALL\",\n  \"target_system\": \"telegram\",\n  \"domain\": \"productivity\",\n  \"risk_hint\": \"external_send\",\n  \"payload\": {\n    \"action\": \"send_message\",\n    \"recipient\": \"Rafi Ahmad\",\n    \"text\": \"deployment completed\"\n  }\n}\n</code></pre> <p><code>recipient</code> is a human-readable reference, not a Bot API address. Before the guardrail asks for approval, AgentGate resolves it against its persistent Telegram contact registry and replaces it internally with a numeric <code>chat_id</code>. The approval view shows the resolved display name and username when known; the connector receives only the numeric chat ID and never guesses from a name.</p> <p>The planner may use <code>chat_id</code> directly only when the user explicitly supplied a numeric Telegram chat ID:</p> <pre><code>{\"action\": \"send_message\", \"chat_id\": 123456789, \"text\": \"deployment completed\"}\n</code></pre> <p>An older plan that puts a name in <code>chat_id</code> is safely reinterpreted as a recipient reference and still requires registry resolution.</p>"},{"location":"integrations/telegram.html#recipient-registration-and-resolution","title":"Recipient Registration And Resolution","text":"<p>Whenever the inbound webhook receives a valid Telegram <code>message.chat</code>, the channel upserts that chat's ID, type, username, first/last name, display name, and first/last-seen timestamps. This happens before any AgentGate run is started, so a recipient pressing <code>/start</code> is enough to become addressable. Duplicate webhook updates remain deduplicated and the database has a unique constraint on <code>chat_id</code>.</p> <p>Resolution is exact and case-insensitive where appropriate:</p> <ul> <li>a numeric chat ID supplied explicitly;</li> <li>an exact <code>@username</code> or username;</li> <li>an exact normalized display name.</li> </ul> <p>AgentGate does not fuzzy-match names or select the first duplicate. An unknown recipient enters the existing <code>ASK_USER</code> flow and no Telegram API request is made. For an ambiguous display name, AgentGate lists the matching display-name/username choices and asks for an exact <code>@username</code> or numeric chat ID. The user must clarify before normal <code>external_send</code> approval begins.</p> <p>Telegram's Bot API cannot globally search Telegram users or DM arbitrary names. The bot can normally resolve a person only after an inbound interaction such as <code>/start</code>, or from a group/channel identity already accessible to the bot. This integration intentionally does not use MTProto, Telethon, or Pyrogram.</p> <p>Because <code>external_send</code> is a side effect, the existing guardrail rules can require approval before the action reaches:</p> <pre><code>ExecutionRouter -&gt; APIExecutor -&gt; TelegramConnector -&gt; Telegram Bot API\n</code></pre> <p>For Telegram-originated runs, approval prompts are sent back to the same chat with inline Approve/Reject buttons. Button callbacks call the existing <code>run_registry.respond(..., action=\"approve\"|\"decline\")</code> path; there is no separate Telegram approval state machine.</p> <p>If a run reaches a sanitize or user-input pause, the bot sends a message telling the user that additional input is required and the run must be resumed through AgentGate.</p>"},{"location":"integrations/telegram.html#manual-end-to-end-check","title":"Manual End-to-End Check","text":"<ol> <li>Rafi opens the AgentGate bot and presses <code>/start</code>.</li> <li>Confirm the webhook accepts the update; this records Rafi's chat identity.</li> <li>In AgentGate Web Chat, enter <code>kirim pesan telegram \"halo\" ke Rafi Ahmad</code>.</li> <li>Confirm the plan has <code>recipient: \"Rafi Ahmad\"</code>, <code>target_system: telegram</code>,    and <code>risk_hint: external_send</code>.</li> <li>Confirm AgentGate resolves the recipient and displays <code>Rafi Ahmad    (@rafiahmad)</code> (when that username is registered) in the approval context.</li> <li>Before approval, verify no <code>sendMessage</code> request has been made.</li> <li>Approve the action. The connector then sends the already-resolved numeric    chat ID, Telegram returns HTTP 200, Rafi receives <code>halo</code>, and the audit trail    records the reference, resolved identity, NEED_APPROVAL/approval transition,    and execution result.</li> </ol>"},{"location":"integrations/telegram.html#production-notes","title":"Production Notes","text":"<ul> <li>Always set <code>TELEGRAM_WEBHOOK_SECRET</code>; an empty secret fails closed.</li> <li>Do not log or expose <code>TELEGRAM_BOT_TOKEN</code> or <code>TELEGRAM_WEBHOOK_SECRET</code>.</li> <li>Telegram HTTP request logs are deliberately credential-free: they record the   method and status (for example <code>sendMessage -&gt; HTTP 200</code>) and redact any   token-bearing Bot API URL emitted by <code>httpx</code>.</li> <li>Register webhooks during deployment, not during FastAPI startup.</li> <li>Use HTTPS only for the public webhook URL.</li> <li>Run a single process for the in-memory MVP deduplication and run registry, or   add shared persistence before scaling horizontally.</li> </ul>"}]}